
# Next session

## สถานะ (อัปเดต 2026-08-24)

รายละเอียดการออกแบบทั้งหมดอยู่ใน **README.md** (DB9 wiring, watchdog, ALPIDE, การวิเคราะห์ latency) ไฟล์นี้เก็บเฉพาะ**สิ่งที่ต้องรู้ก่อนลงมือรอบหน้า**

---

## Checklist ท่ามอง — เพิ่มเข้ามาวันที่ 2026-08-25 เพื่อทดสอบครอบคลุม ไม่ใช่สุ่ม

จากการไล่ session จริงหลายรันพบว่า**พฤติกรรมการมองแบบต่างกันเผยปัญหาคนละจุดกันหมด** — รันที่กวาดตาเร็ว ๆ (`183359`) กับรันที่จ้องนิ่ง (`113659`) ให้ตัวเลขตรงข้ามกันแทบทุกตัว (blink 134/นาที vs 23/นาที, streak median 0.14s vs 2.40s) ถ้าทดสอบแบบสุ่มจะไม่รู้ว่าครอบคลุมพอหรือยัง เอาไว้เป็น checklist ก่อนสรุปค่า config ใด ๆ:

| # | ท่า | ทดสอบอะไร | อ้างอิงจากรันที่เจอ | สถานะ |
|---|---|---|---|---|
| 1 | จ้องนิ่งที่เป้า (~30 วิ) | baseline — `deviation_mm` กระจายแบบไหนตอนไม่มีอะไรผิดปกติ | `session_20260825_113659` | ✅ มีแล้ว |
| 2 | กระพริบตาปกติเป็นระยะ ระหว่างจ้องเป้า | `EAR_BLINK_THRESHOLD` แยกกระพริบจริงจาก noise ได้ไหม (คนจริง 15-20 ครั้ง/นาที, 100-400ms/ครั้ง) | `113659` (23/นาที ใกล้เคียง) | ✅ มีแล้ว |
| 3 | เหลือบไปขอบ threshold ช้า ๆ ค้างไว้ | threshold ตกย่านที่ deviation หนาหรือเปล่า, `min_off`/`dwell` ทำงานถูกจุดไหม | `171132` (threshold 6.0 พาดย่านหนา, สับ 27 ครั้ง) | ✅ มีแล้ว |
| 4 | กวาดตาเร็ว ๆ ไปมุมสุดซ้ำ ๆ | EAR ตกปลอมจากมุมมองไหม (ไม่ใช่ตาปิดจริง), dwell รีเซ็ตถี่แค่ไหน | `183359` (EAR ตกปลอม 58.5% ของ blink สั้น 1 เฟรม) | ✅ มีแล้ว |
| 5 | หลับตานาน (>3 วิ) แล้วลืมกลับมา | แยก `no_face`/`blink` ทำงานถูก, ไม่ trigger false dwell reset ตอนลืมตา | `session_20260825_170143` (EAR<0.20 ถึง 51.1% ของเวลา, blink runs ยาว 4.1s/5.0s/5.4s — `no_face` ไม่โผล่เลยสักครั้ง) | ✅ มีแล้ว |
| 6 | ขยับหัวเล็กน้อย (ไม่หมุนแรง) ระหว่างจ้องเป้า | กล้อง/FaceMesh ยังตามได้ไหม — คนไข้จริงหัวไม่นิ่ง 100% แม้ใส่หน้ากากยึด | `session_20260825_170239` (deviation ปานกลาง p90=10.6mm ไม่สุดโต่งแบบท่า 4, ไม่มี blink ยาวผิดปกติแบบท่า 5, `no_face`=0 — กล้อง/FaceMesh ตามได้ไม่หลุด) | ✅ มีแล้ว |
| 7 | หัวหมุน/เอียงแรงจนหลุดเฟรม | จุดที่ FaceMesh หลุดล็อกแล้วไม่กลับมา — worst case ที่ต้องมี fallback | `180704` (หลุด 10.3 วิ ไม่กลับมาเลย) | ✅ มีแล้ว (โดยไม่ตั้งใจ) |
| 8 | แสงเปลี่ยนระหว่างรัน (หรี่ไฟ/มีเงา) | threshold + EAR ยังเสถียรไหมเมื่อ contrast เปลี่ยน | `session_20260825_171142` (หรี่ไฟ 2 รอบ วัดจริงจาก brightness เฉลี่ยเฟรม: 127→100 แล้ว 127→79 — EAR/iris_px/deviation แทบไม่ขยับเลยทั้งสองรอบ, `no_face`=0, on-target 95-98% ตลอด — ช่วงท้ายรันที่ deviation พุ่ง 174mm เป็นคนละเรื่อง คนขยับตัวออกจากเฟรมหลังไฟสว่างกลับแล้ว ไม่ใช่ผลจากแสง) | ✅ มีแล้ว |

**เพิ่มเข้ามาวันที่ 2026-08-25 (รอบสอง):** ผู้ใช้อัดตามลำดับท่า 1-6 ในตารางนี้ตรง ๆ (`164934`→`165032`→`165925`→`170040`→`170143`→`170239` = ท่า 1→2→3→4→5→6 ยืนยันแล้ว ไม่ใช่แค่เดาจากลักษณะข้อมูล) — ยืนยันเป็นครั้งแรกด้วยว่า `no_face` ไม่โผล่เลยตลอดทั้ง 6 sessions รวมถึงตอนหลับตานาน แปลว่า `_gate_reason()` แยก `blink` (เจอหน้า+EAR ต่ำ) ออกจาก `no_face` (หาหน้าไม่เจอ) ได้ถูกต้องแม้ตาปิดไปหลายวินาที ไม่ใช่แค่ตอนกระพริบสั้น ๆ — ตามด้วยท่า 8 แยกทำต่างหาก (`171142`) ครบทั้ง 8 ท่าแล้ว

**Checklist ครบทั้ง 8 ท่าแล้ว** — เงื่อนไขที่เคยตั้งไว้ก่อนตัดสินค่า default ที่เหลือ (`EAR_BLINK_THRESHOLD`, `min_on_target_s`, `IRIS_MM`) ผ่านแล้ว แต่ข้อมูลทั้งหมดยังมาจากทีมพัฒนาทดสอบ ไม่ใช่คนไข้ที่ใส่หน้ากากยึดจริง — ยังควรระวังตอนสรุปค่าเหล่านั้น ดูข้อ 0 ด้านล่างด้วย

---

## เรื่อง gate policy — ทำไปแล้ววันที่ 2026-08-24 และที่ยังค้าง

### 0. ⚠️ ข้อมูล session ทั้งหมดที่มี **ไม่ใช่ภาพของการใช้งานจริง**

ทุก session ใน `output/` เป็นการทดสอบที่ทีมพัฒนา**ตั้งใจกวาดตาไปมา** ไม่ใช่คนไข้ที่จ้องเป้านิ่ง ๆ ระหว่างฉายรังสี สถิติทุกตัวที่ได้จากมัน (on-target streak median 1.46 s, peak switching 43.6 Hz, duty 18–75%) จึงเป็นภาพของ "คนกำลังทดสอบ"

**ห้ามเอาตัวเลขพวกนี้ไปจูน `min_off_s` / `min_on_target_s` / `threshold_mm`** — ใช้ได้อย่างเดียวคือเป็น **worst case** ("ถ้าตาขยับหนักขนาดนี้ ระบบยังไม่พัง") ค่าจริงต้องรอข้อมูลจากคนไข้ที่จ้องเป้าจริง

### 1. เพิ่ม `min_on_target_s` คู่กับ `min_off_s` (ทำแล้ว) — `DEFAULT_MIN_OFF_S` กลับเป็น 1.0

**หน้าที่ของทั้งคู่คือกันฮาร์ดแวร์ ไม่ใช่บริหารเวลาบีม** — ถ้าไม่มีเลย relay จะตามสถานะตาทีละเฟรม เล่นซ้ำ session ที่มีพบว่าจะถูกสั่งสับสูงสุด **43.6 Hz** shutter พังแน่นอน มันคือ**ตัวจำกัดขอบเขต** ไม่ใช่ตัว optimize (**ตอนแรกวัดผิดประเด็นไปเป็นการแลก "จ่ายบีม vs ลด cycle" ซึ่งกลับหัวกลับหางกับจุดประสงค์** — แก้ทั้งเครื่องมือและเอกสารแล้ว)

**ปัญหาที่พบ:** `min_off_s` นับจาก**ตอนตัด**อย่างเดียว ตาที่กลับเข้ามาที่ 0.9 วิของ hold 1 วิ ทำให้บีมเปิดตอนครบ 1.0 วิ ทั้งที่ตาเพิ่งนิ่งได้ 0.1 วิ — เฟรมถัดไปตาหลุดพอดี relay สับสองครั้งใน ~130 ms **จังหวะที่บีมกลับมาเปิดจึงเป็นเรื่องของโชคล้วน ๆ ว่า hold หมดตรงเฟรมไหน**

**แก้ด้วย `min_on_target_s` = 1.0** นับจาก**ตอนตากลับเข้าเป้า** ต้องนิ่งติดกันครบก่อนถึงเปิด — ตัวไหนช้ากว่าตัวนั้นกำหนด ทำให้การเปิดบีมมาจาก**หลักฐานว่ามันจะเปิดอยู่ได้** ไม่ใช่จากจังหวะที่ hold บังเอิญหมด

| ทางเลือกที่วัด (รวมทุก session) | cycles | **max Hz** | worst ON | beam |
|---|---|---|---|---|
| off 1.0 เดี่ยว (ของเดิม) | 128 | **43.6** | 0.023s | 117.0s |
| off 3.0 เดี่ยว | 88 | **43.6** | 0.023s | 67.5s |
| **off 1.0 + dwell 1.0 (ที่เลือก)** | 104 | **14.3** | **0.070s** | 79.9s |
| off 3.0 + dwell 1.0 | 77 | 22.6 | 0.044s | 51.5s |
| off 3.0 + dwell 3.0 | 27 | 18.2 | 0.055s | 17.1s |

**บรรทัด 1 กับ 2 — `max Hz` เท่ากันเป๊ะ** การขยับ `min_off_s` จาก 1 เป็น 3 **ไม่ลดความถี่สับสูงสุดเลย** แค่ตัดเวลาบีมทิ้ง 40% (เคยเปลี่ยนเป็น 3.0 ไปแล้วรอบหนึ่ง แล้วถอยกลับ) ส่วน dwell ลด 43.6 → 14.3 Hz — **dwell คือกลไกที่ตรงกับปัญหา cooldown ไม่ใช่**

**ทำไม dwell ตั้ง 3 วิไม่ได้:** on-target streak median แค่ **1.46 s** มีเพียง **14.8%** ที่ถึง 3 วิ (คนกระพริบตาทุก 3-4 วิตามธรรมชาติ) ถ้าบังคับ 3 วิบีมเหลือ 17.1s จาก 117s — แต่ย้ำข้อ 0: ตัวเลขนี้มาจากการทดสอบกวาดตา คนไข้ที่จ้องนิ่งจริงจะมี streak ยาวกว่านี้มาก **1.0 วิจึงเป็นค่าเริ่มต้นแบบระมัดระวัง ไม่ใช่ค่าที่จูนมาแล้ว**

### 1a. pulse เปิดสั้นยังหมดไปไม่ได้ — และหมดไม่ได้โดยหลักการ

ขาปิดทันทีเสมอ (ถูกแล้ว) ผลคือตานิ่งครบ dwell → บีมเปิด → เฟรมถัดไปตาหลุดพอดี → ปิดทันที ได้ **pulse เปิดสั้น** ดีที่สุดที่ทำได้คือ `worst ON` **0.070 s** (จาก 0.023 s)

**ทางที่ห้ามทำ: "min beam-ON"** บังคับบีมค้างเปิดขั้นต่ำ = ยิงต่อทั้งที่ตาหลุดแล้ว ผิดหลักความปลอดภัย ไม่ใช่ตัวเลือกที่มีในระบบนี้

**เคยลอง debounce แบบนับเฟรม (3–12 เฟรม) แล้วไม่ได้ผล** — `worst ON` กับ `max Hz` ไม่ขยับเลยแม้ที่ 12 เฟรม เพราะหน้าต่างสั้นเกินไป dwell แบบนับเวลา 1 วิ (≈27 เฟรม) คือสิ่งที่ได้ผล

**ไม่มีกฎฝั่งขาเปิดใดกันได้หมด** เพราะไม่มีกฎไหนทำนายเฟรมถัดไปได้ ทำได้แค่ทำให้เกิดน้อยลง ที่เหลือต้องไปดูสเปกว่า shutter ทนการสับแบบนี้ได้แค่ไหน — **ยังไม่มีใครรู้ว่ากลไกที่ทำให้มันพังคือจำนวนครั้งสะสมหรือความถี่ชั่วขณะ** ซึ่งเป็นตัวตัดสินว่าควรตั้งค่าไหน

### 1c. `171012` — duty 18.3% แต่ไม่ใช่ความผิดของ gate

61% ของรัน (15.1 s จาก 25.6 s) เป็น `blink` ก้อนใหญ่สองก้อน: **10.2 วินาที** และ **4.9 วินาที** — 10 วินาทีไม่ใช่การกระพริบ คือหลับตาค้างหรือ FaceMesh หลุดยาว (`cut_0001` ยืนยันว่าตาปิดสนิทจริง, `cut_0003` เป็น deviation จริง มองไปทางซ้ายสุด 4.87 mm > 4.5)

การกระจายของรันนี้แยกขั้วสุดโต่ง — p50 = 1.2 mm แต่ p90 = 49.9 mm ไม่มีย่านกลางเลย ผลคือ **ตั้ง threshold 3.0 กับ 4.5 ได้ผลเท่ากันเป๊ะที่ 59.9% on-target** และ min_off ลด relay ได้ 0 เพราะไม่มีอะไรให้กระพริบตั้งแต่แรก

บทเรียน: duty cycle ต่ำไม่ได้แปลว่าพารามิเตอร์ตั้งผิดเสมอไป — รันนี้ต่ำเพราะคนหลับตาไป 15 วินาที ซึ่งไม่มีค่าไหนในโค้ดแก้ได้

### 2. `IRIS_MM = 11.0` ใช้ค่าเดียวกับคนไข้ทุกคน

[`config.py`](config.py) แปลง px→mm ด้วยเส้นผ่านศูนย์กลางม่านตาคงที่ 11.0 mm แต่ค่าจริงของมนุษย์ต่างกันราว **10.2–13.0 mm** ระหว่างบุคคล แปลว่า threshold 4.5 mm ที่ operator ตั้ง อาจมีผลจริงเป็น 3.9 mm หรือ 5.1 mm แล้วแต่คนไข้ — คลาดเคลื่อนได้ถึง **±15%**

**อันตรายตรงที่มันมองไม่เห็น:** ทุกเฟรมใน run เดียวกันผิดไปทางเดียวกันหมด ไม่มี jitter ไม่มี outlier ดู `track.csv` กี่ไฟล์ก็ไม่เจอ ต่างจาก noise ที่โผล่ใน p95 ทันที

ตอนนี้ยังไม่เป็นปัญหาเพราะระยะเผื่อกว้าง (ตาอยู่แถว 2-3 mm จาก threshold 4.5 mm) — **แต่ถ้าจะบีบ threshold ให้แคบลงเมื่อไร ต้องแก้ก่อน** วิธีแก้คือวัดม่านตาคนไข้จริงตอน setup แล้วใส่เป็นค่าต่อคน ไม่ใช่ค่าคงที่ในโค้ด

### 2a. บันทึก `ear` ลง `track.csv` แล้ว (ทำแล้ว วันที่ 2026-08-25)

ก่อนหน้านี้ต้อง**เอาวิดีโอมา replay ผ่าน FaceMesh เองทุกครั้ง**เพื่อดูว่า EAR สั่นแค่ไหนตอนเจอ `blink` ผิดปกติ (`183359`, `113659`) ซึ่งไม่ reproducible — FaceMesh แบบ streaming ขึ้นกับว่าจับล็อกได้ตอนไหน รันซ้ำได้ผลต่างจากรันสด (เจอมาแล้วตอนไล่ `180704` ด้วย)

**แก้แล้ว:** `_detect_mp`/`_detect_mp_pupil` เก็บ `self._last_ear` **ก่อน**เทียบกับ `EAR_BLINK_THRESHOLD` เสมอ (ไม่ใช่แค่ตอนไม่เจอ blink) ทำให้เฟรมที่ตัดสินว่า `blink` มีค่า EAR ให้ตรวจสอบย้อนหลังด้วย — คอลัมน์ใหม่ใน `track.csv` ว่างเฉพาะตอน `no_face` ดู README หัวข้อ `track.csv`

**ยังไม่ได้ทำ:** ยังไม่มีรันจริงที่อ่านคอลัมน์นี้ตรง ๆ (ทุกรันก่อนหน้ายังต้อง replay วิดีโอ) — รันหน้าจะตอบได้ทันทีว่า EAR สั่นคาบเส้น 0.20 บ่อยแค่ไหนโดยไม่ต้องเปิดวิดีโอเลย ซึ่งเป็นเงื่อนไขก่อนตัดสิน `EAR_BLINK_THRESHOLD` ตามที่ข้อ 0 บอกไว้

### 3. `no_face` แยกออกจาก `blink` แล้ว (ทำแล้ว)

`_gate_reason()` มี branch `if trigger is None: return 'no_face'` อยู่แล้วแต่**ไปไม่ถึงตลอด** เพราะ detection loop เขียน `trigger = result is True` ซึ่งยุบ `None` (หาหน้าไม่เจอ) กับ `False` (ตาปิด) เป็น `False` ก่อน ทุกอย่างจึงถูกบันทึกเป็น `blink`

**แก้แล้วด้วยการเลิกยุบ** — ส่ง `result` ต่อไปตรง ๆ ทุก consumer ปลายทางรับ `None` ได้อยู่แล้ว (`1 if trigger else 0`, `if trigger:`, `'' if trigger is None else int(trigger)`) **การตัดสินใจเรื่องบีมไม่เปลี่ยนเลยแม้แต่นิดเดียว** เพราะมีแต่ `True` เท่านั้นที่เคยเปิดบีมได้ — `test_no_face_is_not_a_blink` คุมทั้งสองด้าน (แยกป้ายได้ถูก **และ** `None`/`False` ยังปิดบีมเหมือนเดิม)

ผลข้างเคียงที่ตั้งใจ: คอลัมน์ `detect` ใน `track.csv` มีสามค่าแล้ว — `1` / `0` / **ว่าง (หาหน้าไม่เจอ)** ดู README หัวข้อ `track.csv`

### 3a. เคสที่ทำให้ต้องแก้ — `session_20260824_180704` (ยังไม่แก้ต้นเหตุ)

รันนี้ดีมากตลอด 26 วินาที แล้ว **detection ตายสนิท 343 เฟรมสุดท้าย (10.3 วิ)** จน operator กด STOP — บนจอ**วง threshold หายไป** ([capture.py](capture.py) `_render_zoom` วาดวงจาก `_last_iris_px` ซึ่งเป็น `None`) ดูเหมือนแอปค้าง

**ไม่ใช่แอปค้าง** — frame period เร็วขึ้น (38.7→29.7 ms) และ detect+decide เร็วลง (23.7→13.3 ms) ซึ่งเป็นลายเซ็นของ FaceMesh ที่ยังถูกเรียกทุกเฟรมแต่ตอบว่าไม่เจอหน้า และเฟรมดิบจากวิดีโอยืนยันว่า**ตาเปิดอยู่ หัวแค่หมุนไป**

**ต้นเหตุ: มุมกล้องอยู่ริมขอบความสามารถของ MediaPipe** รัน FaceMesh แบบ `static_image_mode=True` (บังคับ detect ใหม่ทุกเฟรม) กับวิดีโอนี้ 18 จุด **เจอหน้าแค่ 1 จุด** — รวมช่วงที่แอปทำงานได้ปกติด้วย แปลว่าที่มุมนี้ MediaPipe **detect แทบไม่ได้เลย ทำได้แค่ track** พอจับล็อกได้ครั้งหนึ่งก็ตามต่อทีละเฟรม พอหลุดล็อกก็ต้อง detect ใหม่ ซึ่งทำไม่ได้ → **ไม่มีทางกลับมาได้ตลอดรันที่เหลือ**

**หลักฐานที่น่ากังวลที่สุด: ผลไม่ reproducible** เอาวิดีโอไฟล์เดียวกันรันซ้ำผ่าน FaceMesh streaming ตั้งค่าเดียวกัน ได้ `none:130 FOUND:18 none:402 FOUND:419` — **ทำงานได้ดีใน 419 เฟรมสุดท้ายซึ่งเป็นช่วงที่รันสดตาย** ผลขึ้นกับว่าบังเอิญจับล็อกได้ตอนไหน ไม่ได้ขึ้นกับภาพ

**บีมทำถูกทุกประการ** — ตรวจไม่เจอ = ปิดบีม ตลอด 10.3 วินาที `state=S` ความปลอดภัยไม่เคยเสีย ที่เสียคือ availability และ operator ไม่รู้ว่าเกิดอะไรขึ้น

**ยังไม่ได้ทำ:**
1. **แจ้ง operator** — ตอนนี้จอเงียบสนิท ควรมี banner เมื่อ `no_face` ติดกันเกิน N วินาที (ตอนนี้เขียนได้แล้วเพราะแยกป้ายออกจาก `blink` แล้ว)
2. **บังคับ re-acquire** — สร้าง FaceMesh instance ใหม่เมื่อ `no_face` ติดกันนาน เพื่อรีเซ็ตสถานะ tracker **ยังไม่ได้ทดสอบว่าช่วยจริงไหม** และการทดลองข้างบนชี้ว่าอาจเป็นแค่การทอยลูกเต๋าใหม่ ไม่ใช่การแก้
3. **ถอยกล้องออกให้เห็นหน้าครบ** — แก้ที่ต้นเหตุจริง แลกกับ `iris_px` ที่จะลดจาก ~140-165 px

### หมายเหตุเรื่องขอบเขต

ทุกข้อข้างบนสรุปจาก session สั้น ๆ ของทีมพัฒนาเอง (19-30 วิ, นั่งนิ่ง, ไม่มีโปรตอนจริง, ALPIDE ส่วนใหญ่ error) — พูดได้แค่ว่า "run เหล่านี้โอเค" ยังพูดไม่ได้ว่า "ระบบโอเค"

**มุมกล้องปัจจุบันดีขึ้นจริง ไม่ต้องขยับอีก** เทียบไล่มาตามเวลา:

| | 20260820 | 154030 | 171012 / 171132 |
|---|---|---|---|
| `iris_px` | 92–112 | 142 | **162 / 164** |
| สเกล mm/px | 0.12 | 0.0775 | **0.0679 / 0.0671** |
| sd ของ `iris_px` | 12.9 | 9.3 | 19.6 / 13.9 |
| detect+decide | 31 ms | 20.5 ms | **14.3 / 19.7 ms** |

(sd ที่โตขึ้นใน `171012` มาจากช่วงหลับตายาว 15 วินาทีที่ลากค่าเพี้ยน ไม่ใช่กล้องแย่ลง — ดูข้อ 1b)

---

## สิ่งที่เพิ่มเข้ามาวันที่ 2026-08-24 — แก้บั๊ก ITS3 ค้าง, วิดีโอ annotated, และบทเรียนจาก Pupil (Close-up) ที่ลองแล้วถอนออก

### เก็บไว้จริง (push ขึ้น `main` แล้ว — commit `18ecd37`)

- **แก้บั๊ก: ปิดแอปเร็วเกินไปทิ้ง session ITS3 ค้าง** — `on_close()` เดิมยิง `_alpide_stop()` แบบ fire-and-forget แล้วปิดโปรแกรมทันที แต่ `eudaq.stop()` (ของ Kcmh-Tricker) นอน 1+5 วินาทีก่อนจะยิง `tmux kill-session` จริง — ถ้าโปรแกรมปิดตัวเร็วกว่านั้น (เกือบทุกครั้ง) thread ที่กำลังนอนอยู่โดนตัดตอน คำสั่งปิด tmux เลยไม่เคยถูกยิงออกไปจริง session ค้างไว้แบบไม่มีใครรู้ ตอนนี้ `_alpide_stop()` return worker thread ออกมา แล้ว `on_close()` `join()` รอ (สูงสุด `config.ALPIDE_STOP_JOIN_TIMEOUT_S = 9.0` วิ) ก่อนปิดโปรแกรมจริง — **จุดปิดอื่น (STOP/CANCEL DAQ) ยังเป็น fire-and-forget เหมือนเดิม** เพราะโปรแกรมยังเปิดอยู่ต่อ ไม่มีความเสี่ยงเรื่อง process ตายกลางคัน
- **วิดีโอบันทึกได้ 2 ไฟล์แล้ว** — ไฟล์ `_annotated.mp4` คู่กับไฟล์ดิบเดิม มีวงกลม detection/crosshair เป้า/inset ติดอยู่ในวิดีโอเลย (เฟรมเดียวกับที่ operator เห็นบนจอ) เขียนผ่าน background thread + bounded queue แยกจาก loop หลัก (ทดลองเขียนแบบ synchronous ก่อน วัดได้ `achieved_fps` ตกจาก ~26 เหลือ ~16 ทันที) **REVIEW ยังเปิดแค่ไฟล์ดิบ** ต้องเปิด `_annotated.mp4` เองถ้าอยากดู overlay
- **`test_beam_gate.py` ปิด session ITS3 ค้างให้เองหลังรันจบ** — เช็คแค่ตอน `stale` (เศษ tmux ที่ตายแล้ว) เท่านั้น ถ้า `running` (producer ครบ 6 ตัว) จะไม่แตะ แค่เตือนไว้ เพราะแยกไม่ออกว่ามีคนกำลัง acquire ข้อมูลจริงอยู่หรือเปล่า

### กับดักที่เสียเวลาไปแล้ว — ลองแล้วถอนออก: Pupil (Close-up) mode + MediaPipe iris landmark model

ลองเพิ่มโหมดตรวจจับที่ 3 สำหรับกล้องซูมเฉพาะตา (ไม่มีหน้าเต็มเฟรม ใช้ FaceMesh ไม่ได้) ทำได้จริงระดับหนึ่ง (threshold-based limbus detection + MediaPipe's `iris_landmark.tflite` refine ตำแหน่ง) แต่สุดท้าย**ผู้ใช้สั่งถอนออกทั้งหมด**หลังเจอปัญหาไล่ไม่จบสักที — โค้ดตอนนี้ไม่มีร่องรอยอะไรเหลือเลย (`detection.py`/`README.md` ตรงกับ git HEAD เป๊ะ) แต่บันทึกบทเรียนไว้เผื่อมีคนอยากลองใหม่:

1. **Threshold-based detection (ไม่มี ML) ใช้งานได้จริงในระดับหนึ่ง** — หา limbus จาก percentile ของความมืด + morphological close ทนกับแสงสะท้อนได้พอสมควร แต่มีจุดอ่อนถาวร: full-frame reacquire (ตอนตาหลุด/กระพริบ) ไม่มีอะไรค้ำประกันว่าจะเจอตาจริง บางครั้งหลุดไปเกาะคิ้ว/ขนตาแทน — เป็น failure mode ที่มีอยู่แล้วในแนวทางนี้ ไม่ใช่บั๊กที่แก้ได้ง่ายๆ
2. **MediaPipe's `iris_landmark.tflite`** (โมเดลที่แถมมากับแพ็กเกจ `mediapipe` อยู่แล้ว ที่ `mediapipe/modules/iris_landmark/`) รับภาพตา crop 64x64 ตรงๆ ไม่ต้องมี face landmark ก็เรียกได้ — แต่**ทางการ MediaPipe เอง sizing crop จากความกว้างมุมตาถึงมุมตา (จาก face landmark) x 2.3** ไม่ใช่จากม่านตา เราไม่มี face landmark เลยต้องเดาสัดส่วน crop เอาเอง (ใช้รัศมีก้อน threshold x 2.0) ซึ่ง**ไม่เคยอิงข้อมูลจริงเลย** — นี่คือรากของปัญหาแทบทั้งหมดที่เจอ (ตำแหน่งเยื้อง, เส้นขอบตาที่โมเดลตอบดูไม่สมส่วน)
3. **บั๊กที่ไล่เจอระหว่างทาง (แก้ได้แต่กว่าจะรู้ตัวเสียเวลามาก):** ผูก "ตำแหน่ง crop" กับ "ขนาด crop" เข้าด้วยกันจนเกิด oscillation ทุกเฟรม (สลับไปมาระหว่างวงใหญ่/วงเล็ก), ใช้ผลลัพธ์ของโมเดลเองมา validate ตัวเองจนดริฟท์ไปล็อกผิดจุด (ไฝ/รอยบนผิวหนัง) แบบไม่มีทางรู้ตัว, ไฟล์ debug ที่ไม่มี log จับคู่กับเฟรมจริงทำให้ดีบั๊กผิดเฟรมโดยไม่รู้ตัว — ทุกอันแก้ไปได้ทีละจุดแต่รวมกันแล้วใช้เวลานานมาก
4. **ถ้าจะลองใหม่ ควรเริ่มจาก:** หาสัดส่วนกว้างตา:เส้นผ่านศูนย์กลางม่านตาจากงานวิจัย/มาตรฐานสรีระจริงก่อน (ไม่ใช่เดา) แล้วค่อย derive ขนาด crop จากตรงนั้น — หรือพิจารณาใช้กล้อง IR/monochrome จริงแทนกล้องแสงปกติ (คุยกันไว้ตอนต้นว่าโมเดล dark-pupil ต้องการ contrast แบบ IR ซึ่งกล้อง Lumix G95 ที่ใช้อยู่ให้ไม่ได้)

---

## สิ่งที่เพิ่มเข้ามาวันที่ 2026-08-20 — RESUME BEAM, dialog ตามธีม, และตามล่าเรื่องคอม lag

จุดเริ่ม: ทดสอบ Manual resume ที่เพิ่งทำ แล้วผู้ใช้สังเกตว่า**เปิดแอปแล้วบางทีคอมทั้งเครื่อง lag เมาส์/คีย์บอร์ดกดไม่ได้** ไล่จนจบพบว่าเป็นปัญหาจริง วัดได้ตัวเลขชัดเจน แก้ไปได้ ~40%

### RESUME BEAM — บีมที่ตาตัดจะไม่เปิดเองจนกว่าจะกดยืนยัน

- **checkbox "Manual resume after cut"** (default ปิด) ใต้ Min beam-off — ติ๊กแล้วบีมที่ถูกตัดเพราะตาหลุดเป้าจะ**ไม่เปิดเองอีกแม้ตาจะกลับเข้าเป้าแล้ว** ต้องกดปุ่ม **RESUME BEAM** (เขียว, โผล่เฉพาะตอน latch ติด) เอง — ใช้กลไก latch แบบเดียวกับ KILL BEAM แต่คนละ flag (`auto_latched` vs `kill_latched`) เพื่อให้ `cuts.csv`/`track.csv` แยกเหตุตัดได้ถูก
- **บั๊กที่จับได้ระหว่างทดสอบเอง (แก้แล้ว):** ตอนแรก RESUME BEAM ปลด latch ทันทีที่กด ไม่สนว่าตาอยู่ในเป้าหรือเปล่า — กดตอนตายังหลุดอยู่แล้วเดินจากไป พอตาเผลอกลับเข้าเป้าทีหลัง **บีมเปิดเองโดยไม่มีใครยืนยันจริง** ขัดกับจุดประสงค์ทั้งหมด แก้ด้วย `CaptureThread._process_resume_request()`: กดตอนตาหลุดเป้า **ทิ้งคำขอเงียบๆ ไม่จดจำไว้รอ** ต้องกดใหม่ตอนตาอยู่ในเป้าจริง — พร้อม dialog เตือนทันทีถ้ากดตอนยังไม่พร้อม (ไม่ใช่เงียบแบบเดิม)

### Dialog เตือนทั้งแอปเปลี่ยนมาใช้ธีมเดียวกับแอป

ผู้ใช้ทักว่า dialog เตือน (เช่นตอนกด RESUME BEAM ไม่ทัน) หน้าตาเป็นของ OS ไม่ตรงธีม — `tkinter.messagebox` เป็นแบบนั้นเสมอ ไม่รับสีที่แอปกำหนดเอง แก้ทั้ง 9 จุดในแอปพร้อมกัน (`_ack_dialog()` ใหม่ ก็อปแพทเทิร์นจาก `_confirm_disable_dialog` ที่มีอยู่แล้ว) — ดู README หัวข้อ "Dialog เตือน"

### ตามล่าเรื่องคอม lag — วัดจริงทุกขั้น ไม่เดา

รายละเอียดตัวเลขทั้งหมดอยู่ใน README หัวข้อ "ประสิทธิภาพ CPU" สรุปสั้นๆ:

- **สาเหตุ:** MediaPipe FaceMesh สร้าง thread pool เท่าจำนวนคอร์ (16) โดย default และแอปสร้างสองตัวพร้อมกันตลอดช่วง preview (ก่อนกด ENABLE ด้วยซ้ำ) — วัดได้ ~330-345% CPU, ~101 threads ตอน idle preview เฉยๆ
- **แก้ 4 จุด:** `OMP_NUM_THREADS=2` (main.py, ต้องตั้งก่อน import app), throttle grayscale cross-check 3→30 เฟรม, **`config.GRAY_CROSSCHECK_ENABLED`** (checkbox ใหม่ ปิดได้ทั้งตัว ไม่ใช่แค่ throttle — มีผลตอนเปิด preview ครั้งถัดไป), **`config.CAPTURE_DOWNSCALE`** (resize ทั้ง pipeline ครั้งเดียวหลัง `cap.read()` — detection/display/recording/cuts ใช้เฟรมเล็กเดียวกันหมด ไม่มี UI เพราะเป็นการตัดสินใจ campaign-level), **`config.PREVIEW_FPS`** (ช่องกรอกใหม่ default 8 — throttle เฉพาะตอน `not armed` เท่านั้น **ไม่กระทบ latency ที่วัดจริงเลยแม้แต่นิดเดียว** เพราะข้าม branch นี้ไปตรงๆ ตอน armed)
- **ผลรวม:** 330-345% → ~200-204% (ปิด gray cross-check + downscale 0.5 พร้อมกัน) ลดได้จริง ~40% แต่ชนเพดาน ~190-200% ที่ไม่ขึ้นกับตัวแปรพวกนี้แล้ว (โปรไฟล์ Python/Tk/GC/MediaPipe เอง)
- **ทดสอบยืนยันความถูกต้องแล้ว:** อัดวิดีโอจริงที่ `CAPTURE_DOWNSCALE=0.5` ไฟล์เล่นได้ถูกต้อง ขนาดตรง ไม่พัง (จุดเสี่ยงเดียวคือ `start_recording()` ต้องรู้ขนาดที่ resize แล้ว ไม่ใช่ขนาดกล้อง native — แก้แล้วด้วย `_scaled_frame_size()` ตัวเดียวที่ทั้งสองจุดใช้ร่วมกัน กันไม่ให้ปัดเศษไม่ตรงกัน)
- **ค่า default ที่เปลี่ยนจริงตอนนั้นมีแค่ `PREVIEW_FPS=8`** — `CAPTURE_DOWNSCALE`/`GRAY_CROSSCHECK_ENABLED` ยังเป็นค่าเดิม (1.0 / เปิด) รอทีมตัดสินใจ **(อัปเดต 2026-08-25: `GRAY_CROSSCHECK_ENABLED` เปลี่ยนเป็น `False` แล้ว หลังมีรายงานเมาส์/คีย์บอร์ดกระตุกตอนเปิดแอปตรงกับอาการที่วัดไว้ตรงนี้เป๊ะ — ดูหัวข้อด้านล่าง `CAPTURE_DOWNSCALE` ยังเป็น 1.0 เหมือนเดิม เพราะกระทบความละเอียด `iris_px` โดยตรง ไม่ใช่แค่ CPU)**

### เจอกล้องที่ใช้จริงคือ Panasonic LUMIX DC-G95

ต่อผ่าน Elgato Cam Link 4K (`/dev/video0`, รายงาน 1920×1080@50fps) — G95 มีแค่โหมดวิดีโอ 4K/FHD ไม่มี 720p ให้เลือก เพราะงั้น**ลดความละเอียดที่ตัวกล้องไม่ได้** ต้องทำในซอฟต์แวร์ (`CAPTURE_DOWNSCALE`) ต่อไป — เทียบกับ paper จริงของระบบ eye-tracking ในคลินิก proton therapy อื่น (CNAO ฯลฯ) พบว่าเขาใช้กันแค่ **512×640 px @ 6-25 Hz** ต่ำกว่าที่โปรเจกต์นี้ใช้อยู่มาก (แต่เขาใช้ stereo/fringe-pattern ซับซ้อนกว่าการแปลง px→mm ตรงๆ แบบที่นี่ เทียบกันตรงๆ ไม่ได้ 100%)

### ยังไม่ได้ทำ

- **`CAPTURE_DOWNSCALE` ยังไม่ได้เปิดใช้จริง** เพิ่งวัดผลไว้ รอทีมตัดสินใจ — `GRAY_CROSSCHECK_ENABLED` ปิดเป็น default แล้ว (2026-08-25) ดูหัวข้อถัดไป

## รายงาน "คอมค้าง" (เมาส์/คีย์บอร์ดกระตุกชั่วครู่) — ระบุสาเหตุแล้ว วันที่ 2026-08-25

ผู้ใช้รายงานว่าบางทีคอมกระตุก/ค้างชั่วครู่ตอนเปิดโปรแกรม — จังหวะตรงกับ [app.py:154](app.py#L154): 200ms หลัง mainloop เริ่ม แอปเปิดกล้อง preview อัตโนมัติ ซึ่งสร้าง FaceMesh สองตัวพร้อมกัน ตรงกับปัญหาที่วัดไว้แล้วข้างบน (~200-345% CPU sustained)

**แก้แล้ว:** `config.GRAY_CROSSCHECK_ENABLED` เปลี่ยนจาก `True` เป็น **`False`** เป็น default — ปิดการสร้าง FaceMesh ตัวที่สองไปเลยตอน preview เปิด **ไม่กระทบตรรกะบีมหรือการวัดตำแหน่งตาเลย** เพราะ gray cross-check ไม่เคยเป็น redundancy จริงตั้งแต่แรก (กล้องตัวเดียวกัน เฟรมเดียวกัน แค่ทำเป็นขาวดำ) และไม่มีอะไรอ่านผลลัพธ์ไปตัดสินใจเรื่องบีม เปิดกลับได้จาก checkbox "Grayscale cross-check" ถ้าต้องการดูค่าเปรียบเทียบ

**ยังไม่ได้ทำ / ยังไม่ยืนยัน:**
- ยังไม่ได้วัด CPU ซ้ำหลังปิด default เพื่อยืนยันว่าอาการกระตุกหายจริง (คาดว่าน่าจะช่วยเพราะตรงกับตัวเลขที่เคยวัดไว้ แต่ยังไม่ได้ทดสอบซ้ำหลังแก้ครั้งนี้)
- `capture.py` มี fallback `self.params.get('gray_crosscheck', True)` เป็น `True` ซึ่งไม่ตรงกับ config default ใหม่แล้ว — ไม่กระทบการใช้งานจริงเพราะ `app.py` เซ็ต key นี้ให้เสมอก่อน `CaptureThread` ทำงาน (ค่านี้เป็นแค่ safety fallback เผื่อเรียก `CaptureThread` ตรง ๆ โดยไม่ผ่าน UI เช่นใน test) แต่ยังไม่ได้แก้ให้ตรงกันเผื่อสร้างความสับสนทีหลัง
- ถ้ากระตุกยังไม่หาย ตัวต่อไปที่ควรลองคือ `CAPTURE_DOWNSCALE` (แต่กระทบ `iris_px` — ต้องชั่งน้ำหนักกับความละเอียดตามที่คุยกันไว้)
- **Raspberry Pi + Camera Module 3 แยกทำ redundancy จริง** (ยังไม่เริ่ม) — ถ้าทำจะได้สองอย่างพร้อมกัน: ลด CPU เครื่องหลัก (ย้าย gray cross-check ออกไปเลย) + ได้ฮาร์ดแวร์อิสระจริงแทนที่จะเป็นแค่รันซ้ำบนภาพเดียวกัน ประเมินไว้คร่าวๆ ~1 สัปดาห์ถ้าแค่โชว์เทียบ ไม่เอาไป gate บีมจริง (ถ้าเอาไป gate ต้อง validate เพิ่มอีกมาก)
- **ยังไม่ได้ลองด้วยตาจริงต่อเนื่องนานๆ** ว่า Preview FPS=8 กระตุกรำคาญไหม — ทดสอบสั้นๆ ผ่าน แต่ยังไม่ได้ใช้งานจริงยาวๆ
- ลบ `video/1.MP4` เก่าทิ้งแล้ว (ใช้ไม่ได้/ล้าสมัย) — `test_pupil.py` default path ชี้ไปที่นั่นตอนนี้จะหาไฟล์ไม่เจอถ้าไม่ใส่ argument เอง

## LAUNCH DAQ กดไม่ติด — เจอ ITS3 session ค้างจริง แก้ต้นเหตุแล้ว วันที่ 2026-08-25

ผู้ใช้รายงานว่ากด LAUNCH DAQ ไม่ได้ — ตรวจ `tmux ls` บนเครื่องจริงพบ session `ITS3` ค้างอยู่ (สร้างตั้งแต่ 12:32:29) มี ALPIDEProducer ครบ 6 ตัวแต่ `ITS3DataCollector` ค้างที่ `UNCONFIGURED` (ไม่เคยขึ้น RUNNING) — heuristic ใน `alpide_daq.session_state()` นับแค่จำนวน producer (`>= config.ALPIDE_NUM` = running) เลยเข้าใจว่า session นี้ "กำลัง acquire อยู่จริง" แล้วปฏิเสธ LAUNCH ใหม่ทุกครั้งด้วย `'ITS3 session busy'`

**ไล่ log สอง session ที่มีเจอเส้นเวลาที่ทำให้ค้าง:**

1. `12:32:29` — LAUNCH DAQ spawn producer/collector ปกติ
2. `12:33:10` (40 วิให้หลัง, ตรงกับ `wait_for_running(timeout=40.0)` เป๊ะ) — ไม่มีบอร์ดจริงต่ออยู่ (ตรงกับ `no_boards found` ที่เจอทั้งวัน) collector เลยไม่เคยขึ้น RUNNING → launch fail
3. `12:33:30` — มีอะไรเรียก `_daq_teardown()` (ถอด ENABLE หรือกด CANCEL DAQ) → `_alpide_stop()` เริ่ม background thread เรียก Kcmh-Tricker's `eudaq.stop(pid)` ซึ่ง sleep รวม ~6 วิ **ก่อน** จะสั่ง `tmux kill-session` จริง
4. **แอปปิดตัวไปในช่วงนั้นเอง** (ไม่มี log อะไรอีกใน session นี้เลย) — `_daq_teardown()` ไม่เคย `join()` รอ thread นี้ จึงตัดจบ thread กลางคันก่อนคำสั่งฆ่า session จะทันได้รัน
5. session ที่ตามมาเลยชน `'ITS3 session busy'` ทุกครั้งที่ลอง LAUNCH ใหม่ (9 ครั้งใน 10 วิ)

**นี่คือบั๊กตัวเดียวกับที่แก้ไปแล้วสำหรับ `on_close()`** เมื่อวันก่อน (commit `18ecd37`, "Wait for ITS3 teardown before exit") — ตอนนั้นแก้แค่จุดปิดโปรแกรมปกติด้วย background thread + `join(timeout=9.0)` ส่วน `_daq_teardown()` (ถอด ENABLE / CANCEL DAQ / Arduino reset — 3 จุดเรียก) ยังปล่อยไว้แบบ fire-and-forget โดยอ้างว่า *"โปรแกรมยังเปิดอยู่ต่อ ไม่มีความเสี่ยงเรื่อง process ตายกลางคัน"* — **สมมติฐานนั้นพิสูจน์แล้วว่าผิดในเคสนี้จริง**

**แก้แล้ว — เปลี่ยนกลไกทั้งหมด ไม่ใช่แค่เติม join():** เทียบกับ Kcmh-Tricker เองพบว่ามันเรียก `eudaq.stop(pid)` **ตรง ๆ บน UI thread แบบ synchronous** ไม่มี thread ไม่มี timeout — ได้ความชัวร์เพราะ Qt/Tk เป็น event loop เธรดเดียว ระหว่างที่ยัง block อยู่จะประมวลผล event "ปิดโปรแกรม" ไม่ได้เลย ไม่มีทาง race ได้ตั้งแต่โครงสร้าง (จุดที่ต้องรู้: Kcmh-Tricker ก็ไม่เคยตรวจสอบว่า `tmux kill-session` ได้ผลจริงเหมือนกัน — ความชัวร์ของมันมาจากไม่มี race ให้เกิด ไม่ใช่มีการยืนยันผลลัพธ์ที่ดีกว่า)

เดิมกลัวว่า sync call จะบล็อก detection loop — **แต่ `CaptureThread` รันอยู่คนละ thread เสมอ และบีมถูก disarm/`E0` ไปก่อนจุดนี้แล้วทุกครั้ง** เลยไม่กระทบความปลอดภัยบีมเลย จึงกอปปี้กลไกของ Kcmh-Tricker ได้ตรง ๆ: `_alpide_stop()` เพิ่มพารามิเตอร์ `sync=True` เรียก `stop_run()` ตรงบน caller's thread แทนการสร้าง background thread — `_daq_teardown()` (ทั้ง 3 จุดเรียก) และ `on_close()` เปลี่ยนมาใช้ `sync=True` ทั้งคู่ ส่วน `stop()` (ปุ่ม STOP ปกติ ถูกกดบ่อยกว่ามาก) **ยังคง async เหมือนเดิม** ไม่ต้องการความชัวร์ระดับนี้เพราะไม่ใช่จุดที่โปรแกรมมักจะปิดตามทันที

**ผลคือง่ายกว่าเดิม ไม่ใช่ซับซ้อนกว่า** — ไม่มี `join()`, ไม่มี timeout ให้เดา, ลบ `config.ALPIDE_STOP_JOIN_TIMEOUT_S` ทิ้งเพราะไม่มีใครใช้แล้ว รอจนกว่าคำสั่งจะถูกส่งออกไปจริง ไม่ใช่รอไม่เกิน N วิ — **Trade-off เดิมยังอยู่:** ถอด ENABLE/กด CANCEL DAQ ตอนมี DAQ ค้างจริงจะค้างหน้าจอ **~6 วินาทีแน่นอน** (จากเดิม "อาจจะ" ค้างถึง 9 วิ) กรณีปกติที่ไม่เคย LAUNCH เลยไม่หน่วงเลย

**ทดสอบแล้ว:** `test_beam_gate.py` ทั้งชุด (รวมเทสต์ LAUNCH/CANCEL DAQ ที่ต้องมีจอจริงผ่าน Xvfb) และ `test_cut_capture.py` ผ่านหมด — ระหว่างแก้เจอ test stub 3 จุด (`_trace_alpide_stop`, `_traced_alpide_stop`, lambda ใน `test_auto_record`) ที่ mock `_alpide_stop()` ไว้แบบไม่รับ `sync` kwarg ต้องแก้ signature ให้ตรงด้วย — เคลียร์ session ที่ค้างจริงบนเครื่องด้วย `tmux kill-session -t ITS3` แล้วยืนยันว่า `pgrep ALPIDEProducer` กลับมาเป็น 0 ทั้งก่อนและหลังรันเทสต์

**ยังไม่ได้ทำ:** `alpide_daq.session_state()` ยังแยก `'running'`/`'stale'` จากแค่จำนวน producer ไม่ได้เช็คว่า collector ไปถึง RUNNING จริงหรือยัง — เคสนี้ผ่านไปได้เพราะแก้ที่ต้นตอ (ไม่ปล่อยให้ค้างตั้งแต่แรก) แต่ตัว heuristic เองยังมีช่องว่างเดิมอยู่ ถ้าเกิด session ค้างจากสาเหตุอื่นที่ไม่ผ่าน `_daq_teardown()`/`on_close()` (เช่น process ถูก `kill -9` ตรง ๆ) ก็จะยังชนปัญหาเดิม

## `alpide-daq-program` ค้าง 3.5 ชม. ยึด USB บอร์ดไว้ — เจอ timeout ของ Kcmh-Tricker เอง พอร์ตมาแล้ว วันที่ 2026-08-25

หลังแก้เรื่อง ITS3 session ค้างข้างบนแล้ว LAUNCH DAQ ยังกดไม่ติดอีกรอบ — รอบนี้คนละสาเหตุ: `lsusb` เจอบอร์ดครบ 6 ตัวจริง แต่มี **`alpide-daq-program` (ตัวแฟลชเฟิร์มแวร์ FX3/FPGA) ค้างมา 3.5 ชั่วโมง** (`ps`: state `S`, 0% CPU, เปิด `/dev/bus/usb/001/0[15-20]` ทั้ง 6 ค้างไว้แบบ exclusive) เริ่มทำงานตอน 12:32:08 — 21 วินาทีก่อน ITS3 session ตัวแรกที่วุ่นกันทั้งวัน น่าจะเป็นจังหวะที่บอร์ดเพิ่งมีไฟ/เสียบใหม่ (ก่อนหน้านั้นทั้งวันเจอ `"no boards found"` เพราะบอร์ดยังไม่ได้เสียบ)

**เจอต้นเหตุในซอร์สของ Kcmh-Tricker เอง** — `modules/ui/firmware_toast.py` เขียนตรง ๆ ว่า *"มี Cancel + hard timeout เพราะถ้าบอร์ด ALPIDE ไม่ re-enumerate กลับมา alpide-daq-program จะค้างใน select() รอ udev event ตลอดกาล"* — บั๊กนี้เป็นที่รู้กันอยู่แล้วในทีม Kcmh-Tricker ไม่ใช่เรื่องแปลก **`FIRMWARE_TIMEOUT_S = 180`** (3 นาที) พร้อม `terminate()` → `wait(3s)` → `kill()` เป็น safety net ที่มีอยู่แล้วในนั้น — eye_tracking's `alpide_daq.install_firmware()` เรียกตัวเดียวกันแต่ **ไม่มี safety net นี้เลย** `proc.wait()` เปล่า ๆ ไม่มี timeout จึงค้างได้ไม่จำกัดเวลา (3.5 ชม. ในเคสนี้ จนต้อง `kill` มือ)

**แก้แล้ว — พอร์ตกลไกเดียวกับ Kcmh-Tricker มาตรง ๆ** (`FIRMWARE_TIMEOUT_S = 180` ค่าเดียวกัน ไม่ใช่ค่าที่เดาใหม่): เพิ่ม watchdog thread ใน `install_firmware()` ที่ `proc.wait(timeout=180)` — ถ้าเกินเวลา สั่ง `terminate()` แล้ว `wait(3)` แล้ว `kill()` ถ้ายังไม่ตาย ผลคือค้างได้สูงสุด **183 วินาทีเท่านั้น** (จากเดิมไม่จำกัด) แล้วรายงาน `on_done(False)` พร้อมข้อความ log ชัดเจนว่า timeout ไม่ใช่เงียบหายไป

**เหตุผลที่ต้องใช้ watchdog thread แยก ไม่ใช่แค่ครอบ `proc.wait(timeout=...)` เฉย ๆ:** loop อ่าน `for line in proc.stdout:` เองก็เป็นจุด block อีกจุดที่ timeout เดียวไปไม่ถึง — ต้อง kill process จริงถึงจะไปปิด pipe แล้วทำให้ loop นั้นคืนค่ากลับมาเอง

**ทดสอบแล้ว:** เพิ่ม `test_firmware_watchdog_kills_a_hang` + `test_firmware_ordinary_exit_is_not_a_timeout` ใน `test_its3_monitor.py` — จำลองด้วย fake script แทน `alpide-daq-program` จริง (ตัวหนึ่งค้างด้วย `exec sleep 9999`, อีกตัวจบปกติ) ไม่ต้องมีบอร์ดจริงก็เทสต์ได้ ยืนยันว่า kill ทำงานภายในเวลาที่กำหนด และไม่ false-positive กับ process ที่จบเร็วปกติ — ทั้ง `test_beam_gate.py`/`test_cut_capture.py`/`test_its3_monitor.py` ผ่านหมด ไม่มี process/session หลงเหลือจากการเทสต์

**ไม่รู้แน่ชัดว่าทำไมบอร์ดถึง re-enumerate ไม่สำเร็จตอน 12:32** (บอร์ดเสีย, สายหลวม ฯลฯ) — **ไม่ใช่เพราะ LAUNCH DAQ ชนกับการแฟลช** ตามที่เคยสงสัยไว้ตอนแรก: เช็คโค้ดแล้ว `_launch_daq()` ที่ [app.py:2381](app.py#L2381) กัน `self._alpide_busy` ไว้อยู่แล้ว (`if self._alpide_pid is not None or self._alpide_busy: return`) กด LAUNCH DAQ ระหว่างแฟลชจึงเป็นแค่ no-op เงียบ ๆ ไม่ได้ไปแตะ USB เลย — ข้อสงสัยเดิมตกไป ยังไม่รู้สาเหตุจริงของการ re-enumerate พัง

### ผลตามมา — FPGA ไม่เคยถูกแฟลชเลย ต้องแฟลชมือ (ทำแล้ว ยังไม่ได้ verify ผ่านแอปจริง)

เพราะสคริปต์แฟลชทำ FX3 ก่อนแล้วค่อย FPGA ต่อ (สองขั้นเรียงในไฟล์เดียว) พอค้างที่ขั้น FX3 (re-enumerate) **ขั้น FPGA เลยไม่เคยเริ่มเลย** — แต่ FX3 เอง re-enumerate สำเร็จได้เองในที่สุด (ไม่รู้ว่าตอนไหน) ทำให้ `alpide_daq.status()`/Kcmh-Tricker's `is_programmed()` เห็นว่า "ready" ทั้งที่ FPGA ยังไม่มี bitstream — LAUNCH DAQ ครั้งต่อมาที่ 16:22 เจอ **`ALPIDE_plane_0..5` ทั้ง 6 ตัวพร้อมกันรายงาน `--ERROR-- Init Error`** ตรงกับทฤษฎีนี้

**ยืนยันแล้วว่าเคยใช้ได้จริง** — session 2026-08-19/20 (5 sessions) มี `alpide_error: None` พร้อมข้อมูลจริง (`session_20260820_124759`: 43,290 events) ไม่ใช่ว่าพังมาตลอด แค่บอร์ดไม่ได้เสียบ/มีไฟตั้งแต่ 24 ส.ค. เป็นต้นมา พอมีไฟใหม่วันนี้ (25 ส.ค.) ไปเจอบั๊ก auto-flash ค้างพอดี

**วิธีแก้เมื่อเจอ `Init Error` แบบนี้อีก — ยืนยันแล้วว่าได้ผลจริง:**
```python
import alpide_daq
alpide_daq.install_firmware()   # ข้าม FX3 อัตโนมัติ (โปรแกรมแล้ว), แฟลช FPGA ทั้ง 6 บอร์ดพร้อมกัน
```
รันจริงวันนี้ (2026-08-25 16:3x) — เสร็จใน ~12 วินาที ไม่ค้าง (`FPGA programming DONE`, `on_done(True)`) เพราะบอร์ด re-enumerate สำเร็จอยู่แล้วตอนนั้น **สาเหตุที่ต้องรันมือ:** `_alpide_prepare()` เช็คแค่ `status() == 'unprogrammed'` ก่อนจะแฟลชอัตโนมัติ พอ FX3 โผล่มา "ready" (ทั้งที่ FPGA ว่างเปล่า) แอปจะไม่มีวันแฟลชซ้ำเองอีกจนกว่าจะเสียไฟใหม่จริง ๆ — **Kcmh-Tricker มีจุดบอดเดียวกันเป๊ะ** (`alpide.is_programmed()` เช็คแค่ USB VID/PID เหมือนกัน) ไม่ใช่ว่า eye_tracking ขาดอะไรที่ Kcmh-Tricker มี

**⚠️ ยังไม่ได้กด LAUNCH DAQ ในแอปจริงเพื่อ verify หลังแฟลช** — แฟลชผ่าน CLI สำเร็จแล้ว (`ok=True`) แต่ยังไม่มี session ใหม่ในแอปหลังจากนั้นเลย ต้องลองจริงในแอปก่อนถึงจะมั่นใจว่ากลับมาใช้ได้ 100%

**ตั้งใจไม่แก้ (ตามที่ผู้ใช้ยืนยัน 2026-08-25):** eye tracking + คุมบีมไม่พึ่ง ALPIDE เลย เป็นแค่ best-effort add-on — สองจุดที่เจอแต่ตัดสินใจไม่แก้เพราะไม่คุ้ม:
1. **ปุ่ม LAUNCH DAQ ไม่โชว์สถานะ "กำลังแฟลชอยู่"** — `_refresh_flow()` ไม่เช็ค `_alpide_busy` เลย ทำให้ปุ่มโชว์ "พร้อมกด" ทั้งที่กดไปจะเป็น no-op เงียบ ๆ (function กันไว้แล้ว ปลอดภัย แค่ UX สับสน)
2. **`status()`/`is_programmed()` เช็คแค่ FX3 ไม่เช็ค FPGA จริง** — คือต้นเหตุที่ต้องมาแฟลชมือรอบนี้ แก้ได้แต่ต้องมีวิธี verify FPGA state เพิ่มซึ่งซับซ้อนกว่าที่มีอยู่

ถ้าจะพึ่ง ALPIDE บ่อยขึ้นในอนาคต ค่อยกลับมาดูสองข้อนี้

---

## สิ่งที่เพิ่มเข้ามารอบสามของวันที่ 2026-08-19 — session folder อธิบายตัวเองได้ + เริ่มวัด latency ฝั่งแอป

จุดเริ่ม: ถามว่า "ตาหลุด → กี่ ms กว่าจะส่งสัญญาณบอกว่าบีมหยุด" ไล่โค้ดแล้วพบว่า **`analyze_latency.py` วัดแค่ครึ่งหลังของโซ่** (Arduino รับคำสั่ง → โปรตอนหยุด) ส่วนครึ่งแรก (ตาขยับ → Arduino รับคำสั่ง) ไม่มีข้อมูลใดคำนวณย้อนหลังได้เลย ทั้งที่โค้ดบันทึกเองว่าเคยวัดได้ 27 fps จากกล้องที่อ้าง 50 — เทอมนี้น่าจะใหญ่กว่าที่กำลังวัดอย่างพิถีพิถันอยู่ด้วยซ้ำ

- **`track.csv`** ใหม่ทั้งไฟล์ — log ทุกเฟรมตลอดช่วง armed (`t_capture`, `t_decided`, deviation, gate, reason) `t_decided - t_capture` คือ latency ฝั่งแอปต่อเฟรม (detect+decide) ที่ไม่เคยมีตัวเลขนี้มาก่อน เขียนตรงจาก loop ไม่ผ่าน queue+thread (มีเทสต์วัดต้นทุนต่อแถวว่า < 0.5 ms) เพิ่ม `_gate_reason()` แยกจาก `_gate_open()` **ไม่แตะของเดิมเลย**
- **`debug/app.log`** — `launch.sh` ไม่ redirect stdout เปิดจาก desktop icon แล้ว `[ALPIDE]`/`[BEAM]`/`[Arduino]` หายหมด ตอนนี้ `self.log()` แทน `print()` ทุกจุด บัฟเฟอร์ไว้ตั้งแต่ก่อนมี session folder (ring buffer 2000 บรรทัด) แล้วดัมพ์ลงไฟล์ตอนโฟลเดอร์ถูกสร้าง
- **`analyze_latency.py` รันให้เองหลัง STOP** เป็น subprocess บน worker thread ที่ `stop_run()` เพิ่งคืนค่ามา เขียน **`report.txt`** (ข้อความที่เคยพิมพ์ออกจอ) + **`analysis.json`** (ตัวเลขอ่านด้วยโปรแกรมได้ + `verdict`) **ทุกครั้งไม่ว่าจะจบทางไหน** รวมถึงตอน exception ที่ไม่คาดคิด — ระหว่างทางเจอบั๊กจริง: `read_beam_events()` crash ด้วย `FileNotFoundError` ดิบๆ ถ้าไม่มี `beam_events.csv` (run ที่ launch แล้วไม่ START) ตอนนี้แปลงเป็น `SystemExit` ที่มีข้อความแล้ว
- **ส่วน "latency budget"** ใน report — อ่าน `track.csv` มาบวกกับ `[5]→[6]` ที่วัดจาก `.raw` อยู่แล้ว ได้ตัวเลข closing-leg รวม (`total_close_leg_ms`) เป็นครั้งแรก
- **`session.json` เพิ่ม provenance + `ended`** — `git_commit`/`git_dirty`/`hostname`/`camera.achieved_fps`/`frames`/`beam_transitions`/`cuts_saved`/`cuts_dropped` และ `ended` (`stop`/`enable_off`/`arduino_reset`/`app_closed`) — เดิม run ที่แอปตายกลางคันหน้าตาเหมือน run ปกติ

**ทดสอบกับของจริงแล้วในเซสชันเดียวกัน (มี Arduino + กล้องจริง ไม่มีบีม)** — `session_20260819_153242` และ `session_20260819_154058` ยืนยันว่าทุกไฟล์ join กันได้จริง (`track.csv` เทียบ `cuts.csv` ตรงเป๊ะทุกแถว ทั้ง `reason` และ `deviation_mm`), `latency_budget` ออกมาสมเหตุสมผลครั้งแรก (`detect+decide` median ~18 ms, `frame period` median ~35 ms ↔ `achieved_fps` ~27-28 ตรงกันสองทาง), `verdict: no_beam_signal` ถูกต้องเพราะไม่มีโปรตอน — **ระหว่างตรวจเจอบั๊กจริง 2 ตัว แก้ไปแล้วทั้งคู่:**

  1. **`[ANALYZE]` หายจาก `debug/app.log`** — `_auto_analyze()` รันบน background thread หลัง `stop()` ปิด `app.log` handle ของ session นั้นไปแล้ว (ต้องรอ `eudaq.stop()` sleep ~6 วิ) เขียนไม่ทัน handle เลยถูกปิดไปก่อน หรือแย่กว่านั้นคือไปเขียนใส่ session ถัดไปผิดไฟล์ แก้ด้วย `log_to_folder()` เขียนตรงไปที่ path ของโฟลเดอร์นั้นเลย ไม่ผ่าน handle ที่ใช้ร่วมกัน (`session_20260819_154058` ยืนยันว่าแก้แล้วเห็น `[ANALYZE] exit 2, verdict=no_beam_signal` ใน app.log จริง)
  2. **`cuts.csv` ติดป้าย `reason` ผิดตอนบีมดับเพราะ disarm ไม่ใช่ตา** — `_queue_cut()` เดิมดู `trigger`/`deviation` อย่างเดียว ไม่เช็ค `armed`/`kill_latched` เลย ของจริงจับได้ที่ `cut_0007.jpg` ของ `session_20260819_154058`: กด STOP ตอนตาอยู่บนเป้าพอดี (`deviation_mm=0.6875` ต่ำกว่า threshold 6.0 มาก) แต่ CSV บอก `reason=deviation` ทั้งที่ `track.csv` เฟรมเดียวกันบอกถูกว่า `unarmed` แก้ด้วยการรวม `_queue_cut()` ให้เรียก `_gate_reason()` ตัวเดียวกับ `track.csv` (เพิ่มพารามิเตอร์ `gate_open`) ตอนนี้สองไฟล์ขัดกันไม่ได้อีกแล้ว มีเทสต์ `test_reason_is_why_the_beam_actually_went_off` คุมไว้

**ยังไม่ได้ทำ — ต้องมีฮาร์ดแวร์:** `[1]→[3]` (sensor exposure + USB transfer ก่อนซอฟต์แวร์เห็นเฟรม) วัดจากซอฟต์แวร์ไม่ได้เลย วิธีที่ถูกที่สุดคือ **LED ในเฟรมกล้อง**: ให้ Arduino ติด LED เองพร้อมประทับ pulse count (เหมือน `beam_events.csv`) แล้วให้แอปตรวจจับ LED ในเฟรม + ประทับเวลาแบบเดียวกับ `t_capture` — ผลต่างคือ [1]→[3] ทั้งก้อน ด้วยนาฬิกาเดียวกับที่ใช้อยู่แล้ว ไม่ต้องสมมติอะไรเพิ่ม ต้องต่อ LED จริงและแก้ firmware เพิ่ม รอบหน้าค่อยทำ

---

## สิ่งที่เพิ่มเข้ามาวันที่ 2026-08-19

มาจากการนั่งเทียบ flow กับ Kcmh-Tricker ทีละปุ่ม

- **แก้บั๊ก: LAUNCH ครั้งที่สองไม่ทำอะไรเลยเงียบๆ** — run ที่ launch แล้วไม่ได้ start ไม่มีทางถอย (STOP กดได้เฉพาะตอน armed, UNREADY เรียก `stop()` เฉพาะเมื่อ armed) `_alpide_pid` จึงค้าง และ `_launch_daq()` เช็คตัวนี้เป็นเงื่อนไขแรกแล้ว `return` **ปุ่มสว่างเป็นขั้นที่ต้องกดแต่กดแล้วไม่เกิดอะไร** อาการเดียวกับซาก tmux ที่เคยเสีย run 59 วินาที ตอนนี้ UNREADY เรียก `_daq_teardown()` เสมอ
- **ปุ่ม CANCEL DAQ** (= `Cancel` ของเขา) โผล่เฉพาะตอนมีอะไรให้ยกเลิก **ไม่แตะฝั่งบีมเลยแม้แต่บรรทัดเดียว** — เทสต์ assert ว่าไม่มีอะไรถูกส่งไป Arduino
- **START กดไม่ได้ระหว่าง `launching`** ขึ้น `waiting for ITS3…` (= `Waiting for ITS3...` ของเขา) + `bell()` ตอนพร้อม **ไม่ขัดกับกฎ 3 วินาที เพราะการรอเกิดที่ LAUNCH ตอนเซ็ตอัพ ไม่ใช่ที่ START** — และมี `_launch_watchdog()` คืนปุ่มให้ที่ 60 วิ ถ้า launch ไม่ตอบเลย detector ห้ามขัง gate
- **`beam used  N.N s`** ใต้ป้าย BEAM — นับเฉพาะเวลาที่ชัตเตอร์เปิดจริง ลง `session.json` เป็น `beam_on_s` **เวลาบนนาฬิกาไม่ได้บอกว่าใช้บีมไปเท่าไหร่** เพราะเรา gate อยู่
- แก้ README ตรงที่เคยเขียนว่า run เราไม่มีจุดจบ — **มี คือบีมที่ขอมาต้องยิงให้หมด** ช่วงทดลองขอสั้นๆ 10-20 วินาที แต่จุดจบนั้นเป็นของ TSS (นับ MU) ไม่ใช่ของแอป

**ยังไม่ได้ทำ:** `Load Run` (plan CSV) ยังไม่มีฝั่งเรา และยังไม่มีช่องกรอกว่า "ขอบีมมากี่วินาที" เพื่อให้ `beam used` เทียบกับเป้าได้ — ตอนนี้โชว์ตัวเลขสะสมเฉยๆ

## สิ่งที่เพิ่มเข้ามารอบสองของวันที่ 2026-08-19 — เอา UI ให้ตรง Kcmh-Tricker มากขึ้นอีกขั้น

- **READY → ☑ ENABLE checkbox จริง** ไม่ใช่ปุ่มทาสีให้ดูเหมือน checkbox อีกต่อไป (`_on_enable_toggle`, `self.enable_var`) — tk flip ตัวแปรให้ก่อนเรียก handler เสมอ ดังนั้น precondition ไม่ผ่าน/ยกเลิก dialog ต้อง `.set()` กลับเอง (ไม่ trigger ซ้ำ)
- **START/STOP/CANCEL DAQ ซ่อนเป็นกลุ่มจนกว่าจะกด LAUNCH DAQ** (`self._run_group`, `show_group` ใน `_refresh_flow`) ตรงกับที่ Kcmh-Tricker ซ่อน Run/Stop/Cancel ไว้จนกด Launch default — **ปุ่มยังไม่หายไปเฉยๆ** มีปุ่มเล็ก **`start without DAQ`** แยกไว้ต่างหากสำหรับตอน ALPIDE ใช้งานไม่ได้ (บอร์ดหาย/flash พัง/tmux ชนกับ Kcmh-Tricker) — ปรึกษาผู้ใช้ก่อนทำเพราะขัดกับกฎที่เขียนไว้ตรงๆ ว่า "ALPIDE ห้ามเป็นเงื่อนไขของการ gate บีม" คำตอบคือซ่อนแถวหลักไว้ตามที่ขอ แต่เก็บทางออกฉุกเฉินแยกไว้ ไม่ลบทิ้ง
- **เอาปุ่ม PAUSE ออก** — ใช้ได้เฉพาะโหมด Video (debug-only) และ disabled ตลอดในโหมดกล้อง (คลินิก) `pause_event` ยังอยู่ในโค้ด แค่ไม่มี UI ไปเคลียร์มันแล้ว (video playback ยัง seek ได้ด้วย slider)
- **RECORD → checkbox `Record video (mp4)` ที่ตั้งครั้งเดียว** ไม่ใช่ปุ่มกดทุก run — **LAUNCH DAQ เป็นคนสั่งอัดให้** (`_maybe_auto_record()`) ปุ่ม `start without DAQ` ก็เรียกซ้ำเผื่อข้าม LAUNCH หยุดที่ STOP/CANCEL DAQ เท่านั้น (`_stop_rec()` ตัวเดียว ใช้ร่วมกัน) **เอา popup "Saved" ออก** เพราะตอนนี้มันโผล่ทุก run ไม่ใช่การกดมือนานๆ ครั้ง
- renumber ปุ่มใน FLOW: `1 LAUNCH DAQ → 2 START → 3 STOP` (ENABLE ไม่มีเลขแล้ว เหมือน checkbox ของ Kcmh-Tricker)
- เทสต์ใหม่ใน `test_beam_gate.py`: `test_daq_row_visibility` (กลุ่ม/fallback โผล่ถูกจังหวะ), `test_auto_record` (checkbox ไม่ trigger เอง, LAUNCH/fallback trigger, CANCEL หยุดให้)

**ยังไม่ได้ทำ:** ยังไม่ได้ลองบนจอจริง (ทดสอบผ่าน Xvfb + screenshot เท่านั้น) — รอบหน้าควรเปิดแอปจริงเช็ค checkbox/fallback button ให้คลิกง่าย ไม่เล็กเกินไป

**ค้างคาใจ:** `\xFE`/`\xEF` ของ Kcmh-Tricker ถูกใช้สองที่ที่ความหมายดูสวนกัน — MANUAL.txt:148 กับ dialog ตอน disable บอกว่า `\xFE` = หยุดบีม แต่ `ProgressWorker` ส่ง `\xFE` ตอน**เริ่ม** exposure step แล้ว `\xEF` ตอนหมดเวลา อ่านจากโค้ดอย่างเดียวแยกไม่ออก อาจเป็นคนละสายสัญญาณกัน ถ้าถามทีม FPGA ได้ควรถาม (README ของเราตอนนี้อิงการอ่านแบบหลัง)

---

## สิ่งที่เพิ่มเข้ามาวันที่ 2026-08-18

- **`analyze_latency.py`** — แปลง session folder เป็นตัวเลข latency คำสั่งเดียว พร้อม `test_latency.py` ที่ครอบส่วนซึ่งข้อมูลไร้โปรตอนพิสูจน์ไม่ได้ (ดูข้อ 1 ด้านล่าง)
- ระหว่างทางพบว่า **pyeudaq บนเครื่องนี้ไม่มี `StdEventConverter`** — ถ้าไปเจอวันมีบีมคือเสีย beam time ฟรี แก้ด้วยการ port decoder เป็น Python (README.md)
- session เก่า 7 จาก 8 อันใน `output/` **วิเคราะห์ไม่ได้** — 6 อันไฟล์ CSV เก่าเกินไป (ไม่มีคอลัมน์ `trig_running` จึงแยกไม่ออกว่า pulse ไหนเป็นเลขค้าง) อีก 1 อันไม่มี `.raw` เลย เครื่องมือบอกเหตุผลรายอันให้แล้ว
- **ITS3 monitor ในแอป** — แถบจุด 6 planes ใน sidebar โชว์ตลอด + แถบ `ITS3` ใต้ภาพกล้อง (ยุบไว้เป็นค่าเริ่มต้น) แสดง RunControl แบบเดียวกับ Kcmh-Tricker + `debug/eudaq.log` เก็บก่อน tmux session ถูกฆ่า
- **`cuts/` — ภาพหน้าจอทุกครั้งที่บีมดับ** อัตโนมัติตั้งแต่กด START ไม่ต้องกด RECORD พร้อม `cuts.csv` ที่ join กับ `beam_events.csv` ได้ด้วย `host_monotonic`
- **ปุ่ม LAUNCH DAQ** แยกการยก EUDAQ2 ออกจาก START (= `Launch default` ของ Kcmh-Tricker) — ดูตัวเลขที่เสียไปในข้อ 1a
- **ปุ่ม KILL BEAM** เป็น latch ตาเอาชนะไม่ได้ (= `Kill beam` ของ Kcmh-Tricker)
- **`Min beam-off`** — ตัดบีมแล้วต้องดับอย่างน้อย N วินาทีก่อนตาจะเปิดกลับได้ (default 1.0 วิ ตั้ง 0 เพื่อปิด) แก้อาการกระพริบตอนตาอยู่ขอบ threshold **หน่วงเฉพาะขาเปิด ขาปิดทันทีเสมอ**
- **สี FLOW บอกลำดับ** — ปุ่มมีเลข `1 READY → 2 LAUNCH → 3 START → 4 STOP` และสว่างทีละปุ่มเดียวคือขั้นที่ต้องกดต่อ วาดจาก `_refresh_flow()` ที่เดียว (เดิมกระจาย 17 จุด)
- **แก้บั๊ก: STOP ปลด gate ช้าไป 6 คำสั่ง** — capture thread ยัง armed อยู่ระหว่างนั้นและ re-state คำสั่งเดิมทุกเฟรมที่ 10 ถ้าตาอยู่บนเป้าพอดีจะส่ง `B1` **เปิดบีมกลับหลังกด STOP**
- **แก้บั๊ก: กด START ระหว่าง LAUNCH ยังขึ้นไม่เสร็จ = ไม่มี `T1` ทั้ง run** — `_alpide_pid` ถูกตั้งทันทีที่ `start_run()` แต่ `_daq_ready` ตั้งหลัง `wait_for_running()` (~10 วิ) ระหว่างนั้น `_alpide_start()` เข้าเงื่อนไข `_alpide_pid is not None` แล้ว return ทิ้ง ตอนนี้เก็บ hz ไว้ใน `_pending_start_hz` แล้ว `_launch_done()` ยิง `T1` ต่อเมื่อ run ขึ้น (ตัดสินใจบน UI thread ทั้งหมด ไม่มี race) **ไม่ disable ปุ่ม START ระหว่าง launching เพราะกฎ 3 วินาทีสำคัญกว่า detector**
- **จับได้แล้วเมื่อ Arduino รีบูตกลางคัน** — บรรทัด `READY` ตอน boot เคยถูกพิมพ์ทิ้งเฉยๆ ทั้งที่มันแปลว่า Enable ตก, trigger หยุด, ตัวนับพัลส์ถูกล้าง แอปจะยังโชว์ READY/armed ต่อและเก็บข้อมูลที่เทียบเวลาไม่ได้ ตอนนี้ disarm + ส่ง `E0` + `alpide_error = 'Arduino reset mid-run'` + ขึ้น dialog (ไฟดับ 2026-08-18 เป็นตัวเปิดโปง)
- **Theme เดียวกับ Kcmh-Tricker** — light theme ดึงค่าจาก `GLOBAL_STYLE` ของเขา แยกสีเป็นสองตระกูล `*B`/`*L` (พื้นปุ่ม + ตัวหนังสือบนนั้น) กับ `*_TXT` (ตัวหนังสือบนพื้นสว่าง) เพราะบนพื้นขาวใช้ค่าเดียวสองบทบาทไม่ได้
- **ITS3 เป็นหน้าต่างลอย** — แท็บเล็กมุมล่างซ้าย กดแล้วแผ่น RunControl 80×40 ตัวอักษรลอยทับบนภาพ **ภาพกล้องไม่ย่อไม่ขยับ** (`place()` ไม่ใช่ `pack()`) พร้อมสี: ชื่อ plane navy / RUNNING เขียว / ERROR แดง ชุดเดียวกับจุด 6 จุด
- **แก้บั๊ก: sidebar ซ้ายเลื่อนตามลูกกลิ้งจากทุกที่ในแอป** — `bind_all` ผูก wheel ไว้ทั้งแอปพลิเคชัน และ `yview_scroll` ไม่เคยเช็คว่ามีที่ให้เลื่อน ตอนนี้ผูกเฉพาะตอนเมาส์อยู่เหนือ sidebar + ปฏิเสธเมื่อเนื้อหาพอดี + ซ่อน scrollbar
- **`analyze_latency.py` รับมือ plane หลุด sync ได้** — ยื่นแต่ละ plane ด้วย trigger number ของตัวเองแทนที่จะทิ้ง event ที่ไม่ตรงกัน (เดิมทิ้งไป 19,409 จาก 19,411 แล้วสรุปผิดว่า "ไม่มีโปรตอน") timestamp เอาจาก plane กลุ่มเสียงข้างมากเท่านั้น

---

## สิ่งที่เพิ่มเข้ามาใน session 2026-08-17

- **Enable ต้องกด READY ก่อน** — เดิม relay Enable ถูก hardwire ON ตั้งแต่บอร์ดมีไฟ ตอนนี้ต้องมีคนกดถึงจะขึ้น (`E1`/`E0`)
- **Watchdog ในเฟิร์มแวร์** — ไม่ได้รับคำสั่งเกิน 2 วินาที relay ตกสถานะปลอดภัย (แอป crash ไม่ทิ้ง Enable ค้างอีก)
- **ALPIDE acquisition ครบวงจร** — flash firmware → EUDAQ2 → trigger 1 kHz → correlation log อัตโนมัติจากปุ่ม READY/START/STOP
- **ผลการทดลองรวมใน `output/session_<ts>/`** ที่เดียว (alpide/ video/ debug/ + session.json)
- **แก้จอค้าง** — display path 50.5 → 14.8 ms และบั๊กอื่นอีก 3 ตัวที่ให้อาการเดียวกัน

**บั๊กฮาร์ดแวร์ที่เจอและแก้ไปแล้ว:** สายรีเลย์บน custom PCB ต่อผิด (COM ของ Relay A อยู่ที่ขา 4 แทนขา 6) ซึ่งจะช็อร์ต +12V ของ TSS ลง GND ทันทีที่ใช้สถานะ Enable OFF — มองไม่เห็นเลยตลอดที่ Enable ถูก hardwire ค้าง

---

## ⚠️ กับดักที่เสียเวลาไปแล้ว — อย่าให้เสียซ้ำ

1. **`/dev/ttyUSB0` คือ Zaber ไม่ใช่ Arduino** — Arduino เป็น CH340 (`1a86:7523`) ปกติได้ `ttyUSB1` แต่**ลำดับสลับได้ทุกครั้งที่เสียบใหม่** เคย upload firmware ใส่ Zaber มาแล้ว เช็คก่อนทุกครั้งด้วย `lsusb` หรือดู VID/PID

2. **EUDAQ2 รันใน venv ของ eye_tracking ไม่ได้** — ต้องการ `urwid` + `alpidedaqboard` จาก user site-packages ใช้ `alpide_daq.clean_env()` เสมอ **และ tmux pane สืบทอด env จาก server ตัวที่เริ่มไว้ก่อน** ถ้า launch ครั้งแรกด้วย env ปนเปื้อน จะพังซ้ำๆ จนกว่าจะ `tmux kill-server`

3. **บอร์ด ALPIDE กลับเป็น DFU mode ทุกครั้งที่ไฟหลุด/run ถูกตัด** (FX3 firmware อยู่ใน RAM) — แอป flash ให้อัตโนมัติตอน READY แล้ว

4. **`kill -USR1 <pid>` ดัมพ์ stack ของทุก thread** — `main.py` พิมพ์ pid ของตัวเองตอนเปิด ใช้ตอนแอปค้าง อันนี้คือตัวที่ทำให้เจอบั๊ก connect ซ้อนของ Arduino

5. **สาย/ขั้วไฟของ ALPIDE หลวมได้เอง** — วันนี้บอร์ดหลุดหมด 2 ครั้งโดยไม่มีใครแตะ ครั้งหนึ่งเสีย run ไป 59 วินาที เช็คขั้วให้แน่นก่อนเริ่มวัดจริง

6. **อย่าเดาสัดส่วน crop ของ MediaPipe iris model เอาเอง** — ลองทำ Pupil (Close-up) mode ด้วย `iris_landmark.tflite` ไปแล้วรอบ 2026-08-24 (ถอนออกหมดแล้ว) ทางการ MediaPipe sizing crop จากความกว้างมุมตา (face landmark) x 2.3 ไม่ใช่จากม่านตา — ไม่มี face landmark ก็อย่าเดาอัตราส่วนเอง หาสัดส่วนสรีระจริงมาก่อน ไม่งั้นเสียเวลาไล่บั๊ก oscillation/drift/ตำแหน่งเยื้องซ้ำแบบเดิม (รายละเอียดเต็มอยู่ใน "สิ่งที่เพิ่มเข้ามาวันที่ 2026-08-24" ด้านบน)

---

## งานที่ต้องทำต่อ

### 1. ทดสอบด้วยบีมโปรตอนจริง ← สำคัญสุด

**ทุกอย่างที่ทดสอบมาทำโดยไม่มีโปรตอนสักตัว** `.raw` ที่ได้มีแต่เฟรมเปล่า การวัด latency จริงยังไม่เคยเกิดขึ้น

**เครื่องมือวิเคราะห์เขียนเสร็จแล้ว** — พอได้ข้อมูลจริงเหลือคำสั่งเดียว ไม่ต้องมาเขียนโค้ดหน้างาน:

```
python3 analyze_latency.py output/session_<ts>
```

ได้ตาราง latency ต่อ transition + สรุปสถิติ + `latency.csv` ในโฟลเดอร์ session (รายละเอียดการทำงานอยู่ใน README.md)

**เพิ่มเข้ามาวันที่ 2026-08-25 — `plot_beam_overlap.py`** ตอบคำถามเดียวกันแต่เป็นภาพ: กราฟ `deviation_mm` ซ้อนกราฟ hit rate ของ plane ที่เลือกบนแกนเวลาเดียวกัน พร้อมเส้น B0/B1 — เห็นด้วยตาว่า "ตาหลุด → บีมตัด → โปรตอนหยุด" เหลื่อมกันเท่าไหร่ **รันให้เองหลัง STOP เหมือนกัน** (`_auto_plot()` ต่อจาก `_auto_analyze()`) ทดสอบกับข้อมูลวันนี้แล้ว (มี `.raw` จริง ยังไม่มีบีมจริง) — เส้น B0/B1 ตรงกับจุดที่ deviation ข้าม threshold เป๊ะ, fit นาฬิกาคลาด 0.01-0.02%, กราฟ hit rate เป็น noise ไม่มีโครงสร้าง (ถูกต้องเพราะไม่มีโปรตอน) รอบมีบีมจริงถึงจะเห็นว่ากราฟล่างมีขั้นบันไดจริงหรือเปล่า ดู README หัวข้อเดียวกันสำหรับรายละเอียด

**ที่พิสูจน์แล้วด้วยข้อมูลจริงที่มีอยู่:** decode `.raw` ทั้งไฟล์ผ่าน 0 error (292,806 blocks), trigger number ต่อเนื่องและตรงกันทั้ง 6 planes, คาบ trigger = 1000.0 µs เป๊ะ, และรันโหมดปกติแล้วได้ `no beam signal detected` อย่างที่ควรเป็น

**ที่ยังพิสูจน์ด้วยของจริงไม่ได้จนกว่าจะมีโปรตอน:** ตัวจับจังหวะที่ hit หายไป — ทดสอบด้วยข้อมูลสังเคราะห์ไว้แล้วใน `test_latency.py` (`python3 test_latency.py`)

**เช็ควันมีบีม:** กด LAUNCH DAQ แล้ว**รอแถบจุด 6 planes เขียวครบ** — ตอนนี้ปุ่ม START กดไม่ได้จนกว่า run จะขึ้นอยู่แล้ว (`waiting for ITS3…`) แต่จุด 6 จุดยังต้องดูเอง เพราะ `wait_for_running()` เช็คแค่ว่า **มี plane ไหนก็ได้** ขึ้น RUNNING (`'RUNNING' in plane_states(...).values()`) ไม่ได้เช็คว่าครบหก จบ run แรกให้รัน `python3 analyze_latency.py <session> --self-check` ทันทีก่อนเก็บ run ต่อไป — ดู `decode errors` ต้องเป็น 0 และ **`planes out of step` ต้องเป็น none**

### 1a. ⚠️ plane 1 หลุด sync — เกิดๆ หายๆ ยังไม่รู้สาเหตุ

**อาการ:** บอร์ดทั้ง 6 นับ pulse trigger ที่ตัวเองได้รับ แล้วประทับเลขของตัวเองลงทุก readout ปกติต้องตรงกัน `planes out of step` ใน `--self-check` คือจำนวน event ที่ไม่ตรง

**พบใน `session_20260818_132638`** (run จริง 26 วินาที ที่ 1 kHz) — ไม่พบใน `session_20260818_144941` และ `session_20260817_191837`

ไล่ทั้งไฟล์แล้วได้รูปแบบชัดเจนมาก:

```
19,409 events   p0+0  p1+1  p2+0  p3+0  p4+0  p5+0
     2 events   ตรงกันหมด
event แรกที่เพี้ยน: 3

event      3:  p0=2      p1=3      p2=2      p3=2      p4=2      p5=2
event  19000:  p0=18999  p1=19000  p2=18999  p3=18999  p4=18999  p5=18999
```

**offset เป็น +1 เป๊ะตลอด ไม่เคยเป็น +2** → ตัด "noise ต่อเนื่อง" ทิ้งได้ (ถ้าเป็น noise ตัวเลขต้องสะสม) **เป็นเหตุการณ์เดียวตอนเริ่ม run แล้วจบ** เกิดหลัง `T1` ประมาณ 2-3 ms (event 1-2 ยังตรงกันดี)

**ตัดออกไปแล้ว — pulse แรกสั้นผิดปกติ:** `trigStart()` เริ่มนาฬิกาใน `trigApply()` ก่อนแล้วค่อยต่อ `OC4B` เข้า D7 เป็นคำสั่งสุดท้าย มีช่องว่างจริงแต่แค่ ~20-40 cycles ที่ 1 kHz คือ pulse แรกกว้าง 497.5 µs จาก 500 µs = **99.5% ของปกติ** ไม่ใช่ runt ที่จะทำให้บอร์ดตัวเดียวนับเกิน

**สมมติฐานที่เหลือ:**
1. p1 ปล่อย readout เกินมา 1 ครั้งตอน arm — ตัวนับเป็นของ producer ไม่ใช่ของ pulse โดยตรง readout ผีครั้งเดียวทำให้ +1 ค้างตลอด
2. signal integrity ที่แขนงสาย trigger ของ p1 (สะท้อน/ringing เห็น edge เกิน)
3. p1 arm เร็วกว่าอีก 5 ตัวจนคาบ pulse แรกคร่อมกันพอดี

**วิธีแยก:** scope D7 ตอน `T1` เทียบกับสายที่เข้า J1 ของ p1 โดยเฉพาะ แล้วเทียบว่า run ที่หลุดกับไม่หลุดต่างกันตรงไหน

**เบาะแสเพิ่ม:** run ที่ p1 หลุด sync **เป็น run เดียวกับที่ p1 มี noise สูงผิดปกติ** (2.474 hits/trigger เทียบกับ 1.34 ในอีกสอง run) สองอาการมาด้วยกัน → ชี้ไปที่บอร์ด/สาย p1 ตัวนั้น ไม่ใช่ระบบรวม

**ผลกระทบ:** ไม่บล็อกการวัด — `analyze_latency.py` ยื่นแต่ละ plane ด้วยเลขของตัวเองแล้ว p1 เหลื่อม 1 ms ซึ่งเทียบกับ latency ระดับหลายสิบ ms แทบไม่มีผล และตัด p1 ทิ้งก็ยังเหลืออีก 5 plane **แต่ถ้าหลุดมากกว่า 1 หรือหลายๆ plane ข้อมูลจะใช้ไม่ได้จริง**

**อาการเกิดๆ หายๆ แบบนี้จับยากกว่าเกิดประจำ — เช็คบรรทัด `planes out of step` ทุก run**

### 1a-2. Baseline ของ detector (ไว้เทียบวันมีบีม)

noise ต่อ plane นิ่งมากระหว่าง run ที่ไม่มีปัญหา:

| session | p0 | p1 | p2 | p3 | p4 | p5 |
|---|---|---|---|---|---|---|
| `191837` | 0.055 | 1.328 | 0.830 | 0.550 | 2.985 | 0.376 |
| `132638` ⚠️ | 0.069 | **2.474** | 0.929 | 0.641 | 3.046 | 0.517 |
| `144941` | 0.062 | 1.342 | 0.831 | 0.558 | 3.001 | 0.450 |

**บีมต้องแรงแค่ไหนถึงจะจับจังหวะดับได้** (คำนวณจาก noise ของ `144941`):

| plane | noise | ต้องเกิน | = กี่เท่า |
|---|---|---|---|
| **p0** | 0.059 | **0.330** | 5.6× |
| p5 | 0.436 | 1.165 | 2.7× |
| p3 | 0.557 | 1.320 | 2.4× |
| p2 | 0.819 | 1.749 | 2.1× |
| p1 | 1.343 | 2.509 | 1.9× |
| p4 | 3.006 | 4.023 | 1.3× |

**p0 ไวที่สุด** — บีมให้แค่ ~0.33 hit/trigger (1 hit ทุก 3 ms) ก็พอ ส่วน p4 noise สูงสุด ใช้ไม่ได้จนกว่าบีมจะเกิน 4 hits/trigger

### 1b. ผลการทดสอบกับฮาร์ดแวร์จริง 2026-08-18

**`session_20260818_144941` — run ที่สะอาดที่สุดที่เคยได้ ยืนยันว่า LAUNCH ได้ผลจริง**

```
LAUNCH  14:49:41
START   14:49:54.610   (+14 วิ — รอ DAQ ขึ้น)
B1 แรก  14:49:54.643   pulse 3   trig_running=1
```

| session | transition ที่หายไป | transition แรกที่ pulse |
|---|---|---|
| `191837` (ก่อนมี LAUNCH) | 3/19 = 16% | 1142 |
| `132638` (ก่อนมี LAUNCH) | 20/71 = 28% | — |
| **`144941` (มี LAUNCH)** | **0/11 = 0%** | **3** |

ALPIDE: 39,428 events, decode error 0, trigger ขาดหาย 0, คาบ 999.9-1000.0 µs, `planes out of step: none`

`cuts/` join กับ `beam_events.csv` ได้ตรงทุกใบ (5 cuts ↔ 5 B0) ห่างกัน **19.8-23.7 ms สม่ำเสมอ** ซึ่งคือเวลาที่ Arduino echo `EV B0` กลับมาทาง serial ไม่ใช่ความคลาดเคลื่อน และ `reason` แยกได้จริง (1 ใบเป็น `blink` อีก 4 ใบเป็น `deviation` ที่ 6.9-9.1 mm เทียบ threshold 6.0)

> **skew ที่ต้องรู้:** `host_time_iso` ใน `beam_events.csv` คือเวลาที่แอป**อ่าน echo** ไม่ใช่เวลาที่ส่ง `B0` — ช้ากว่าจริง ~21 ms ไม่กระทบการวัด latency (ตัวนั้นใช้ `pulse`) แต่ถ้าเอา timestamp ไปเทียบกับ `cuts.csv` หรือ `frames.csv` ต้องหักออก

**`session_20260818_173023` — run แรกที่ทุกอย่างทำงานพร้อมกัน**

```
22,463 events · planes out of step: none · trig_running=0 เป็นศูนย์ · pulse เริ่มที่ 5
min beam-off 1.0 → ช่วงปิดสั้นสุด 1001 ms · ต่ำกว่า 1 วิ 0 ครั้ง
cuts 12 ใบ ตรงกับจำนวนครั้งที่บีมดับ
```

**`session_20260818_174524` — ลอง `min_off` = 5.0 แล้วได้บทเรียนเรื่อง duty cycle**

ช่วงปิดตรึงที่ 5 วิเป๊ะทุกครั้ง แต่ช่วง**เปิด**กลายเป็น 25-52 ms เพราะตาลอยอยู่ขอบ threshold พอดี (`cuts.csv` โชว์ deviation 6.02-6.68 mm เทียบ threshold 6.00)

| session | thr | min_off | ON | OFF | **duty** |
|---|---|---|---|---|---|
| `160327` | 2.0 | — | 43.0s | 3.6s | **92%** |
| `160107` | 2.0 | — | 37.7s | 15.6s | **71%** |
| `170212` | 3.0 | 1.0 | 7.8s | 16.8s | **32%** |
| `173023` | 6.0 | 1.0 | 8.7s | 12.6s | **41%** |
| `174524` | 6.0 | **5.0** | 4.1s | 25.1s | **14%** |

**การเปิดสั้นๆ ไม่ใช่ปัญหา** — ปลอดภัยถูกต้อง โดสสะสมตามปกติ (respiratory gating ก็ทำงานแบบนี้) **สิ่งที่เป็น trade-off จริงคือ duty cycle**: hold ยาว = รีเลย์สบาย แต่คนไข้ค้างท่านานขึ้นมาก เพราะโดสต้องครบ MU เท่าเดิม ไม่มีข้างไหนถูก ต้องให้ทีมกำหนด

**คำถามเดียวที่ยังตอบไม่ได้:** บีมจ่ายโดสเต็มอัตราภายใน 25 ms หลังเปิด shutter หรือไม่ ถ้ามี rise time การเปิดสั้นจะได้โดสไม่ตรงกับเวลา — **ต้องถามทีม TSS**

**ยังไม่ได้ลอง:**
- **KILL BEAM ตอน armed จริง** — ต้องรอ beam time (ตัวโค้ดผ่านเทสต์แล้ว)
- **เคส run ไม่ขึ้น → ต้องยังได้ `debug/eudaq.log`** ทำให้เกิดได้ 5 แบบ (ค่าที่จะไปโผล่ใน `session.json` → `alpide_error`):

  | `alpide_error` | ทำให้เกิดยังไง |
  |---|---|
  | `6/6 need firmware` | **กด LAUNCH โดยไม่กด READY ก่อน** ← ง่ายสุด |
  | `no boards found` | ถอดสาย USB ของ DAQ แล้ว LAUNCH |
  | `ITS3 session busy` | เปิด Kcmh-Tricker ค้างไว้แล้ว LAUNCH |
  | `never reached RUNNING` | EUDAQ2 ขึ้นไม่สำเร็จใน 40 วิ (env ปนเปื้อน / `tmux kill-server` กลางคัน) |
  | `<Exception>` | อะไรก็ตามที่ throw |

  ทุกเคส run ต้องเดินต่อและ gate บีมได้ปกติ แค่ไม่มี `.raw`
- STOP → LAUNCH → START รอบสอง → ต้องได้โฟลเดอร์ session ใหม่ ไม่ทับของเดิม

### 2. เช็คก่อนใช้งานทุกครั้ง

- **กด LAUNCH DAQ แล้วรอจุด 6 planes เขียวครบก่อนกด START** — ถ้า START โดยไม่ LAUNCH มันจะยก EUDAQ2 เองแบบเดิม ซึ่งเสีย 6-7 วินาทีแรกไป (transition ในช่วงนั้นได้ `trig_running=0` ใช้เทียบข้อมูลไม่ได้ วัดได้ 16-28% ของทั้ง run)
- **ดู `alpide_error` ใน `session.json` ก่อนวิเคราะห์** — ถ้าไม่ใช่ `null` แปลว่า run นั้นไม่มีข้อมูล detector (run ที่ล้มเหลวหน้าตาเหมือน run ปกติทุกประการ เคยเสีย run ไปแล้วเพราะเรื่องนี้)

### 2b. แผนการทดลองการขยับตา — ยังไม่มี ควรมี

`session_20260818_1601xx` เก็บได้ 110 transition แต่**วัดได้จริงแค่ 24%** เพราะช่วงระหว่างคำสั่งสั้นกว่า 200 ms ตัวหา changepoint แทบไม่มีข้อมูลจะทำงาน พอตั้ง `min_off` = 1.0 ขึ้นเป็น 79-91%

| session | min_off | ช่วงระหว่างคำสั่ง (มัธยฐาน) | ใช้ได้ (≥200 ms) |
|---|---|---|---|
| `160107` | — | 64 ms | **24%** |
| `160327` | — | 63 ms | **18%** |
| `170212` | 1.0 | 1012 ms | **91%** |
| `173023` | 1.0 | 1010 ms | **79%** |

เรากำลังวัด**เวลาตอบสนองของระบบ ไม่ใช่พฤติกรรมของตา** ตาจึงควรทำตัวเป็นสัญญาณทดสอบที่สะอาด: **ข้ามเส้นเด็ดขาดอย่าเลียบ → ค้างนอกเป้า 3 วิ → กลับเข้ามาลึกๆ ค้าง 5 วิ → ทำซ้ำ 20 รอบ ≈ 2.7 นาที** ใช้เมโทรนอมกำกับ ตั้ง `min_off` = 1.0 และ **อย่าตั้ง threshold ให้ตาพักอยู่ตรงเส้นพอดี**

แยกเก็บทีละเงื่อนไข: **A** ขยับเร็วและไกล (~2× threshold) = latency พื้นฐาน · **B** ไถลข้ามช้าๆ = detection กินเวลาเพิ่มเท่าไหร่ · **C** กะพริบอย่างเดียว (คนละเส้นทางโค้ดกับ deviation) · **D** ออกซ้าย/ขวา/บน/ล่าง = เรขาคณิต iris ต่างกันไหม

หลังทุก run เช็ค `cuts.csv` ถ้า `deviation_mm` เกาะ threshold แค่ 0.1-0.7 mm แปลว่ารอบนั้นเลียบเส้น ให้ทิ้งแล้วเก็บใหม่

**ทางที่ดีกว่ามาก — ใช้ phantom แทนตาคน:** ปริ้นรูปม่านตาติดบน Zaber แล้วเลื่อนด้วยความเร็วที่ตั้งไว้ จะได้**จุดข้ามเส้นที่คำนวณได้** = ground truth อิสระไปเทียบกับที่ระบบวัด ซึ่งตอนนี้ไม่มีเลย ทำซ้ำได้เป๊ะ และไม่มี bias จากคนทดสอบที่รู้ว่าบีมกำลังจะตัด (`Kcmh-Tricker/modules/motion.py` ขับ Zaber ได้อยู่แล้ว) — ใช้ตาคนไว้ยืนยันตอนท้ายว่า FaceMesh ทำงานกับตาจริง

### 3. ตัดสินใจที่ยังค้าง (ไม่บล็อกอะไร)

**เพดานเวลาที่บีมถูกพักค้างรอตา — ต้องถามนักฟิสิกส์การแพทย์**

Kcmh-Tricker มี `Auto kill beam (10s)` แต่เอามาใช้ตรงๆ ไม่ได้ — run เขาเป็น burst สั้น (`Exposure 2000 ms × 10 loops`) ของเรายิงต่อเนื่อง 45-55 วินาที และ**โดสนับเป็น MU ไม่ใช่วินาที** TSS หยุดเองเมื่อครบ MU ถ้ากระแสบีมแกว่ง เวลาเท่ากันจะได้โดสไม่เท่ากัน timer ในแอปเราจึงไม่ใช่ตัวคุมโดส

คำถามจริงคือคนละตัว: **ถ้าตาไม่กลับเข้าเป้าเลย จะปล่อยให้คนไข้ค้างท่ารอได้นานแค่ไหนก่อนหยุดประเมินใหม่** — อันนั้นเป็นเพดานของ "เวลาที่บีมถูกพัก" ไม่ใช่ "เวลาที่บีมเปิด" และค่าต้องมาจากทีม ไม่ใช่จากโค้ด

(ตอนนี้มี KILL BEAM ให้กดเองแล้ว ซึ่งไม่ต้องรอใครตัดสินใจ)

**ความละเอียดกล้อง — tradeoff ที่ต้องเลือกเอง:**

| | ความแม่น | คาบตัดสินใจ gate |
|---|---|---|
| 1080p (ตอนนี้) | iris ~180 px → 0.06 mm/px | ~66 ms |
| 960×540 | iris ~90 px → ~0.12 mm/px | น่าจะ ~35-40 ms |

คาบตัดสินใจคือ**ตัวใหญ่ที่สุดใน latency ที่จะวัด** ลดความละเอียดจะลด latency ได้เกือบครึ่ง แต่แลกความแม่นไปครึ่งหนึ่ง — ที่ threshold 3 mm ความแม่น 0.12 mm/px ยังเหลือ 25 pixel ซึ่งน่าจะพอ แต่ต้องเป็นคนตัดสินใจว่างานต้องการแค่ไหน

*(ลองมาแล้วไม่ได้ผล: ROI crop รอบใบหน้า — หน้าอยู่ใกล้กล้องจนกินพื้นที่เกือบทั้งเฟรม ลดพื้นที่ได้แค่ 1.7 เท่า ไม่พอ ถอนออกไปแล้ว)*

**Hysteresis สำหรับ threshold:** ตอนตั้ง 3.0 mm บีมกระพริบรัวมาก (สลับ 37 ครั้งใน 9 วินาที มี 19 ช่วงสั้นกว่า 100 ms) พอขึ้นเป็น 8.5 mm หายไป ถ้าจะใช้ threshold ต่ำต้องมี hysteresis (เช่นเปิดที่ < 3.0 mm แต่ตัดที่ > 3.5 mm) — **แต่มันเปลี่ยนพฤติกรรมความปลอดภัย** ยอมให้บีมค้างเปิดต่ออีกนิดตอนตาเริ่มหลุด ต้องเป็นคนกำหนดค่าเอง

### 4. ค้างมาจาก session ก่อน — ยังไม่ได้แตะเลย

**กล้องตัวที่สอง (Raspberry Pi Camera Module 3)** เพื่อ redundancy จริง — ตอนนี้ grayscale cross-check รัน FaceMesh ซ้ำบนภาพขาวดำของ**กล้องตัวเดียวกัน** ซึ่งไม่ใช่ hardware อิสระ ยังไม่ได้ตัดสินใจว่า:
- จะต่อจริงด้วย `picamera2` ไหม (`cv2.VideoCapture` ไม่เสถียรกับกล้อง CSI)
- ถ้าต่อแล้วจะเอาผลต่างของ 2 ค่าไป **gate การยิงบีมจริง** หรือแค่โชว์ให้ operator เทียบเอง

*(หมายเหตุ: cross-check ตัวนี้ถูกข้ามตอน `armed` แล้ว เพราะกิน 27 ms ต่อเฟรมและไม่แตะ trigger เลย — แต่วัดแล้วไม่ได้ทำให้ mean frame rate ดีขึ้น)*

### 5. เรื่องขั้นตอนที่ยืนยันแล้ว

**กด START พร้อม Beam On ที่ console — ห้ามห่างเกิน 3 วินาที** (จากการใช้งานจริงที่ KCMH 3 รอบ ไม่ใช่ spec ที่มีเอกสาร ถ้าได้เอกสารจากทีม TSS ควรมาปรับ)

นี่คือเหตุผลที่ `start()` ต้อง arm ทันที และงาน ALPIDE ทั้งหมดอยู่ใน background thread
